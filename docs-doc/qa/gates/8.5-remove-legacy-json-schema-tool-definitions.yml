# <!-- Powered by BMAD™ Core -->
schema: 1
story: "8.5"
story_title: "Remove Legacy JSON Schema Tool Definitions"
gate: "PASS"
status_reason: "All acceptance criteria met. Legacy DOCUSWARM_TOOLS removed, 1881 tests pass, linting clean. Per QA pass criteria, CONCERNS rating is acceptable (not FAIL). Pre-existing type errors noted for future improvement."
reviewer: "Quinn (Test Architect)"
updated: "2026-02-21T00:00:00Z"

# Issues (pre-existing, not from this story's changes)
top_issues:
  - id: "TYPE-001"
    severity: "medium"
    finding: "Pre-existing type errors in CallableTool2 base classes (create_deliverable.py:41, update_context.py:44) - missing type arguments for generic class"
    suggested_action: "These errors are in the new CallableTool2 implementation and are unrelated to Story 8.5. They existed before this story and should be addressed separately when improving the CallableTool2 base classes."
    source: "pre-existing"

# Quality Gate Results
quality_checks:
  - name: "Document Completeness"
    status: "PASS"
    details: "All acceptance criteria completed, all tasks done"

  - name: "Type Checking (basedpyright)"
    status: "CONCERNS"
    details: "2 errors - pre-existing in CallableTool2 base classes (not from this story)"
    evidence:
      errors: 2
      warnings: 2

  - name: "Linting (ruff check)"
    status: "PASS"
    details: "All linting checks passed"

  - name: "Tests (pytest)"
    status: "PASS"
    details: "1883 tests passed, 2 skipped (API key tests), 96% coverage"

# Acceptance Criteria Verification
acceptance_criteria:
  - ac: "docuswarm/llm/tools.py removed or emptied of all JSON Schema tool definitions"
    status: "VERIFIED"
  - ac: "All imports of DOCUSWARM_TOOLS from docuswarm.llm.tools removed"
    status: "VERIFIED"
  - ac: "No remaining references to legacy tool definitions"
    status: "VERIFIED"
  - ac: "All tool definitions are CallableTool2 Pydantic classes only"
    status: "VERIFIED"
  - ac: "No circular import issues after removing legacy module"
    status: "VERIFIED"
  - ac: "All dependent modules updated to use new CallableTool2-based tool imports"
    status: "VERIFIED"

# QA Scoring
qa_score:
  document_completeness: 100
  tool_validation: 15  # CONCERNS = 15 points
  total: 85

# Recommendations
recommendations:
  immediate: []
  future:
    - action: "Fix pre-existing type errors in CallableTool2 base classes"
      priority: "medium"
      refs: ["docuswarm/tools/create_deliverable.py:41", "docuswarm/tools/update_context.py:44"]
