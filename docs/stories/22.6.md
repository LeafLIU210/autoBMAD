# Story 22.6: 创建 analyst Persona 配置

## Status
**Status**: Ready for Development

**Last Updated**: 2026-03-02 22:26:10

## Story
**As a** AI developer implementing the BMM persona system,
**I want** to create the BMM-aligned persona.json for the analyst node,
**So that** Mary (Analyst) has complete role context for generating product briefs.

## Acceptance Criteria
- [ ] Create `nodes/analyst/persona.json` file with all required fields
- [ ] Verify `name` field equals "Mary"
- [ ] Verify `role` field equals "Strategic Business Analyst + Requirements Expert"
- [ ] Verify `identity` contains description of analyst expertise in market research and requirements elicitation
- [ ] Verify `communication_style` contains treasure hunter excitement metaphor
- [ ] Verify `expertise` array contains at least 6 items including Porter's Five Forces, SWOT analysis
- [ ] Verify `principles` array contains at least 4 guiding principles
- [ ] Verify `output_format` specifies type "product-brief" and format "markdown"
- [ ] Validate JSON syntax is correct
- [ ] Verify backward compatibility with existing Persona loader

## Tasks / Subtasks
- [ ] Task 1: Create `nodes/analyst/` directory if not exists
- [ ] Task 2: Create `nodes/analyst/persona.json` with complete persona configuration
- [ ] Task 3: Validate JSON syntax using `python -c "import json; json.load(open('nodes/analyst/persona.json'))"`
- [ ] Task 4: Run Persona loader test to verify compatibility
- [ ] Task 5: Verify all required fields match Epic specification

## Dev Notes
- **File Location**: The persona.json should be placed in `nodes/analyst/persona.json`
- **Backward Compatibility**: The `communication_style` field must have a default value of empty string in the Persona dataclass to maintain compatibility with older persona.json files that may not have this field
- **JSON Validation**: Must be valid JSON with no trailing commas
- **Field Requirements**: All string arrays (expertise, principles) must have at least one item
- **Output Format**: The output_format defines what type of deliverable this persona produces (product-brief)

## Testing
### Unit Tests
- [ ] Test: Verify persona.json loads without errors
- [ ] Test: Verify all required fields are present
- [ ] Test: Verify name equals "Mary"
- [ ] Test: Verify expertise array contains "Porter's Five Forces"
- [ ] Test: Verify communication_style contains "treasure hunter"

### Integration Tests
- [ ] Integration Test: Load persona.json through PersonaLoader and verify all fields map correctly
- [ ] Integration Test: Generate system prompt using Mary persona and verify Persona section displays correctly

### Manual Testing
- [ ] Manual Test: Run `python -c "import json; print(json.load(open('nodes/analyst/persona.json'))['name'])"` to verify output is "Mary"
- [ ] Manual Test: Run `pytest tests/nodes/test_five_node_personas.py -v` to verify analyst persona configuration

---
*This story template was created by SM Agent and awaits SDK filling.*
