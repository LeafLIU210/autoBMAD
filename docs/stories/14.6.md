# Story 14.6: IndependentAgent 重构

## Status

**Status**: Ready for Done

**Last Updated**: 2026-03-02 03:32:35

## Story
**As a** developer
**I want to** 重构 IndependentAgent 以使用 ToolResultExtractor
**So that** 实现纯工具输出模式，消除对 LLM JSON 输出的依赖

## Acceptance Criteria
- [x] 移除 JSON 解析回退逻辑（agents/independent.py 中无 markdown_fallback）
- [x] 使用 `ToolResultExtractor` 提取元数据，替换原有的 `extract_json` 调用
- [x] 从工具参数构建 `IndependentOutput`，返回 deliverable 和 tool_calls 结构
- [x] 移除 `extract_json` 依赖及其相关代码
- [x] 集成后 IndependentAgent 正常工作，无运行时错误

## Tasks / Subtasks
- [x] Task 1: 修改 `_handle_agent_response` 方法，使用 ToolResultExtractor
- [x] Task 2: 移除 `extract_json` 调用及相关依赖导入
- [x] Task 3: 移除 markdown fallback 逻辑（markdown_fallback 相关代码）
- [x] Task 4: 验证重构后 IndependentAgent 功能正常
- [x] Task 5: 更新 IndependentAgent 的类型注解（如需要）
- [x] Task 6: 运行现有测试确保无回归

## Dev Notes
- **依赖组件**: 需要先完成 US-14.1（ToolResultExtractor 实现）
- **错误处理**: 当 Agent 未调用 create_deliverable 工具时，抛出 `IndependentAgentError`
- **返回值结构**: 必须保持与原有 `IndependentOutput` 兼容的结构
- **SDK 兼容性**: ToolResultExtractor 已处理 Kimi/Claude SDK 格式差异，IndependentAgent 无需关心
- **测试策略**: 重点测试错误场景（工具未调用）和正常工具调用场景

## Testing
### Unit Tests
- [x] Test: 验证 `_handle_agent_response` 正确调用 ToolResultExtractor
- [x] Test: 验证当无工具调用时抛出 IndependentAgentError
- [x] Test: 验证返回的 IndependentOutput 结构正确
- [x] Test: 验证 remove 后 extract_json 导入不再使用

### Integration Tests
- [x] Integration test: 端到端测试 IndependentAgent 完整流程（使用 mock SDK）
- [x] Integration test: 验证 create_deliverable 工具调用能正确提取元数据

### Manual Testing
- [x] Manual test: 运行 agents/independent.py 相关测试套件
- [x] Manual test: 基于pyright 类型检查通过
- [x] Manual test: ruff 代码风格检查通过

## Implementation Summary
- Replaced `extract_json` with `ToolResultExtractor` for deterministic tool output extraction
- Removed markdown fallback logic and related code
- Added proper error handling when no create_deliverable tool is called
- Updated imports to include `ToolResultExtractor` and `DeliverableMetadata`
- Removed unused `_extract_content_from_messages` method
- All 48 tests pass (15 new refactoring tests + 33 tool tests)
- ruff code style check passes

## QA Results
**Reviewer**: Quinn (Test Architect)
**Date**: 2026-03-02
**Gate**: PASS

### Validation Summary
- **Pyright Type Checking**: 0 errors ✅
- **Ruff Linting**: All checks passed ✅
- **Tests**: 15/15 passed ✅

### Acceptance Criteria Verification
| AC# | Criteria | Status |
|-----|----------|--------|
| 1 | 移除 JSON 解析回退逻辑（无 markdown_fallback） | ✅ Verified |
| 2 | 使用 ToolResultExtractor 替换 extract_json | ✅ Verified |
| 3 | 从工具参数构建 IndependentOutput | ✅ Verified |
| 4 | 移除 extract_json 依赖及相关代码 | ✅ Verified |
| 5 | 集成后正常工作，无运行时错误 | ✅ Verified |

### Gate Decision
**PASS** - All acceptance criteria met, implementation verified, all tests pass.

---
*This story was created by SM Agent based on EPIC-14.*
