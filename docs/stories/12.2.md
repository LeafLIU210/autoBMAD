# Story 12.2: get_or_create 方法实现

## Status
**Status**: Ready for Development

**Last Updated**: 2026-03-02 00:49:15

## Story
**As a** developer,
**I want to** implement the get_or_create method in CheckpointManager,
**So that** I can support checkpointer creation and reuse with proper caching and configuration generation.

## Acceptance Criteria
- [x] 返回 `(checkpointer, config)` 元组
- [x] 相同 `pipeline_id` 返回相同 checkpointer 实例
- [x] 不同 `pipeline_id` 返回不同实例
- [x] 使用 `generate_thread_id` 生成 thread_id
- [x] 外部 checkpointer 优先于缓存的 checkpointer
- [x] 首次调用为新的 pipeline_id 创建新的 checkpointer 并加入缓存

## Tasks / Subtasks
- [x] Task 1: 实现 `get_or_create` 方法 - 核心逻辑实现
- [x] Task 2: 集成缓存逻辑 - 检查 _cache 并返回或创建 checkpointer
- [x] Task 3: 集成外部 checkpointer 处理 - 优先使用外部传入的 checkpointer
- [x] Task 4: 集成 `generate_thread_id` 调用 - 生成正确的 thread_id
- [x] Task 5: 集成 `create_checkpoint_config` 调用 - 生成正确的 RunnableConfig
- [x] Task 6: 添加结构化日志 - 记录 checkpointer 获取/创建事件

## Dev Notes
- [Note 1] Checkpointer 必须实现 `BaseCheckpointSaver` 接口
- [Note 2] `RunnableConfig` 需要从 `langchain_core.runnables` 导入
- [Note 3] `generate_thread_id` 函数需要从 orchestrator 同模块导入或自行实现
- [Note 4] 缓存键使用 `pipeline_id` 字符串，确保唯一性
- [Note 5] 外部 checkpointer 优先级最高，不走缓存逻辑
- [Note 6] 如果 external_checkpointer 存在，每次调用都返回同一个实例
- [Note 7] 需要处理 `BaseCheckpointSaver[Any]` 的类型注解以支持泛型

## Testing
### Unit Tests
- [x] Test: test_get_or_create_returns_tuple - 验证返回类型为 (checkpointer, config) 元组
- [x] Test: test_same_pipeline_returns_same_checkpointer - 验证相同 pipeline_id 缓存命中
- [x] Test: test_different_pipelines_get_different_checkpointers - 验证不同 pipeline_id 返回不同实例
- [x] Test: test_external_checkpointer_takes_precedence - 验证外部 checkpointer 优先于缓存
- [x] Test: test_first_call_creates_new_checkpointer - 验证首次调用创建新实例并缓存
- [x] Test: test_thread_id_generation - 验证 thread_id 正确生成

### Integration Tests
- [x] Integration test: test_checkpoint_manager_lifecycle - 验证 get_or_create 与 close 配合工作

### Manual Testing
- [ ] Manual test: 运行完整 pipeline 验证 checkpointer 复用

## Implementation Details
- Implemented `get_or_create` method in `autoBMAD/docuswarm/pipeline/checkpoint_manager.py`
- Added `_thread_id_cache` dict to cache thread_ids per pipeline_id
- Uses `generate_thread_id` from `autoBMAD.docuswarm.storage.checkpoints`
- Uses `create_checkpoint_config` from `autoBMAD.docuswarm.storage.checkpoints`
- Added structured logging for checkpointer creation, cache hits, and external checkpointer usage
- All 16 unit tests pass with 100% pass rate

---
*This story was created by SM Agent based on EPIC-12 requirements.*
