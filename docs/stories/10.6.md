# Story 10.6: Full Pipeline End-to-End Validation

## Status
**Status**: Ready for Development

**Last Updated**: 2026-02-21 19:30:51

## Story
**As a** developer,
**I want to** run a complete 5-node BMAD pipeline through the SDK,
**So that** I can verify the entire system works end-to-end post-migration.

## Acceptance Criteria
- [ ] All 5 BMAD nodes (analyst, pm, ux, architect, po) execute via SDK
- [ ] Each node creates deliverable and receives evaluation
- [ ] Context chaining works across nodes
- [ ] Pipeline state persisted to SQLite
- [ ] LangGraph orchestration functions correctly
- [ ] No KimiClient code paths executed

## Tasks / Subtasks
- [ ] Task 1: Create full pipeline integration test with SDK SessionManager
- [ ] Task 2: Execute all 5 nodes sequentially through the pipeline
- [ ] Task 3: Verify deliverables created for each node (PRD, Sprint Plan, Wireframes, Architecture, Roadmap)
- [ ] Task 4: Verify evaluations returned for each node (score ≥ 7/10 threshold)
- [ ] Task 5: Verify context chaining across nodes (each node receives prior node output as input)
- [ ] Task 6: Verify state persistence and recovery (SQLite state survives restart)
- [ ] Task 7: Verify zero legacy KimiClient code paths are executed (grep check)
- [ ] Task 8: Document pipeline execution time and resource usage

## Dev Notes
- **SDK Integration**: Use `KimiSessionManager` with `single_prompt()` for each node execution
- **Node Configuration**: Load node configs from `config/nodes/` directory
- **State Management**: Use existing SQLite state manager from `docuswarm/state/`
- **LangGraph Flow**: Leverage existing `DualAgentNode` and `LangGraphWorkflow` classes
- **Context Chaining**: Each node receives `context` dict containing prior node deliverables
- **Error Handling**: Pipeline should gracefully handle single node failures without crashing
- **Mock vs Real**: Use SDK mocking for unit tests; require real API key for integration tests

## Testing

### Unit Tests
- [ ] Test: Single node execution returns valid deliverable
- [ ] Test: Context chaining passes correct data between nodes
- [ ] Test: State persistence saves and loads correctly
- [ ] Test: KimiClient is never instantiated during pipeline run
- [ ] Test: LangGraph workflow state transitions correctly

### Integration Tests
- [ ] Integration Test: Full 5-node pipeline executes end-to-end with mock SDK
- [ ] Integration Test: Full pipeline with real SDK (requires API key)
- [ ] Integration Test: Pipeline state recovery after simulated crash
- [ ] Integration Test: Pipeline handles node failure gracefully

### Manual Testing
- [ ] Manual Test: Execute pipeline with sample document set
- [ ] Manual Test: Verify all 5 deliverables are created and valid
- [ ] Manual Test: Check that evaluations are consistent across runs

---
*This story was created by SM Agent (Bob) for EPIC-10 Cleanup & Optimization.*
