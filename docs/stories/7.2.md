# Story 7.2: ModeMapper Implementation

## Status

**Status**: Ready for Done

**Last Updated**: 2026-02-21 10:19:34

## Story
**As a** developer
**I want** to map existing ChatMode values to SDK parameters
**So that** the three operational modes translate correctly to SDK config

## Acceptance Criteria
- [x] `SDKModeParams` dataclass created with `model`, `thinking`, and `max_steps_per_turn` fields
- [x] `MODE_MAP` dictionary implemented with instant/thinking/agent mode mappings
- [x] `map_mode()` function provides safe mode translation with validation
- [x] Unknown ChatMode values raise a clear `ValueError` with descriptive message
- [x] All three ChatMode variants (INSTANT, THINKING, AGENT) map correctly to SDK parameters
- [x] Unit tests verify all mode mappings and error handling for invalid modes

## Tasks / Subtasks
- [x] Task 1: Create `docuswarm/llm/mode_mapper.py` module file
- [x] Task 2: Define `SDKModeParams` dataclass with `model: str`, `thinking: bool`, `max_steps_per_turn: int | None` fields
- [x] Task 3: Implement `MODE_MAP` dictionary mapping ChatMode.INSTANT → SDKModeParams(model="kimi", thinking=False, max_steps_per_turn=5)
- [x] Task 4: Implement `MODE_MAP` dictionary mapping ChatMode.THINKING → SDKModeParams(model="kimi", thinking=True, max_steps_per_turn=10)
- [x] Task 5: Implement `MODE_MAP` dictionary mapping ChatMode.AGENT → SDKModeParams(model="kimi", thinking=False, max_steps_per_turn=50)
- [x] Task 6: Add `map_mode(mode: ChatMode) -> SDKModeParams` function with validation
- [x] Task 7: Implement error handling for unknown/invalid ChatMode values
- [x] Task 8: Write unit tests for all three mode mappings
- [x] Task 9: Write unit tests for invalid mode error handling
- [x] Task 10: Add module docstrings and type hints throughout

## Dev Notes
- The `ChatMode` enum is imported from `docuswarm.llm.base` (existing module)
- `SDKModeParams` should be a `@dataclass` with `frozen=True` for immutability
- The `map_mode()` function should accept `ChatMode` enum and return `SDKModeParams`
- Default fallback behavior: Raise `ValueError` with message "Unknown ChatMode: {mode}" - do NOT silently default
- Consider adding a `get_supported_modes()` helper for debugging/inspection
- Mode mapping values align with Epic 7's defined parameters in Section 3 (Story 7.2)
- Follow Ralph's principles: KISS - keep the mapping simple and explicit

## Testing
### Unit Tests
- [x] Test that ChatMode.INSTANT maps to SDKModeParams(thinking=False, max_steps_per_turn=5)
- [x] Test that ChatMode.THINKING maps to SDKModeParams(thinking=True, max_steps_per_turn=10)
- [x] Test that ChatMode.AGENT maps to SDKModeParams(thinking=False, max_steps_per_turn=50)
- [x] Test that invalid/unknown ChatMode raises ValueError with correct message
- [x] Test that SDKModeParams is frozen/immutable
- [x] Test that map_mode() returns correct SDKModeParams instance for each mode

### Integration Tests
- [ ] Integration test with KimiSessionManager to verify mode parameters are correctly passed

### Manual Testing
- [ ] Manual verification by printing mode mappings for all ChatMode variants
- [ ] Manual test with KimiSessionManager to confirm modes work as expected

## QA Results
**Gate Decision**: PASS
**Reviewed by**: Quinn (Test Architect)
**Date**: 2026-02-21

### Quality Gate Results
| Tool | Status | Details |
|------|--------|---------|
| basedpyright (Type Checking) | PASS | 0 errors, 0 warnings |
| ruff (Linting) | PASS | 0 errors (1 auto-fixed import order) |
| pytest (Tests) | PASS | 14/14 tests passed |

### Acceptance Criteria Verification
- [x] SDKModeParams dataclass created with model, thinking, max_steps_per_turn fields (frozen=True)
- [x] MODE_MAP dictionary implemented with all three mode mappings
- [x] map_mode() function provides safe mode translation with validation
- [x] Unknown ChatMode values raise ValueError with descriptive message
- [x] All three ChatMode variants map correctly
- [x] Unit tests verify all mappings and error handling

### Notes
- Integration tests with KimiSessionManager marked as incomplete in story (not blocking)
- All unit tests pass with 100% coverage on mode_mapper.py

---
*Story created by SM Agent for EPIC-07 Core Layer Transformation*
