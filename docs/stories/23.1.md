# Story 23.1: 移除 loader.py 废弃数据类

## Status
**Status**: Ready for Done

**Last Updated**: 2026-03-02 23:19:28

## Story
**As a** developer,
**I want** to remove deprecated dataclasses from loader.py,
**So that** the codebase is cleaner and only contains used structures.

## Acceptance Criteria

### Primary Criteria
- [x] **AC1**: The loader.py file no longer contains `NodeQuestionConfig` dataclass definition
- [x] **AC2**: The loader.py file no longer contains `NodeQuestionsConfig` dataclass definition
- [x] **AC3**: The loader.py file no longer contains `NodeDependenciesConfig` dataclass definition
- [x] **AC4**: All imports and references to these three dataclasses are removed from loader.py
- [x] **AC5**: basedpyright reports no type errors after removal

### NodeConfig Field Removal
- [x] **AC6**: The NodeConfig dataclass no longer contains the `questions` field
- [x] **AC7**: The NodeConfig dataclass no longer contains the `dependencies` field
- [x] **AC8**: NodeConfig can still be instantiated with remaining fields (name, role, prompt, model, temperature, max_tokens)
- [x] **AC9**: No code in the codebase references the removed `questions` or `dependencies` fields on NodeConfig

### Verification
- [x] **AC10**: `python -c "from docuswarm.nodes.loader import NodeConfig; print(NodeConfig.__dataclass_fields__.keys())"` shows only valid fields
- [x] **AC11**: `grep -r "NodeQuestionConfig\|NodeQuestionsConfig\|NodeDependenciesConfig" autoBMAD/docuswarm/` returns no results

## Tasks / Subtasks

### Task 1: Remove Deprecated Dataclass Definitions
- [x] **T1.1**: Open `autoBMAD/docuswarm/nodes/loader.py`
- [x] **T1.2**: Remove `NodeQuestionConfig` dataclass (if exists)
- [x] **T1.3**: Remove `NodeQuestionsConfig` dataclass (if exists)
- [x] **T1.4**: Remove `NodeDependenciesConfig` dataclass (if exists)
- [x] **T1.5**: Remove any associated type hints or helper functions for these classes

### Task 2: Update NodeConfig Dataclass
- [x] **T2.1**: Locate `NodeConfig` dataclass definition
- [x] **T2.2**: Remove `questions` field from NodeConfig
- [x] **T2.3**: Remove `dependencies` field from NodeConfig
- [x] **T2.4**: Verify NodeConfig has required fields: name, role, prompt, model, temperature, max_tokens

### Task 3: Clean Up Imports and References
- [x] **T3.1**: Check loader.py for any unused imports after removal
- [x] **T3.2**: Search entire codebase for references to removed dataclasses
- [x] **T3.3**: Update or remove any imports of removed dataclasses from other files

### Task 4: Type Checking and Validation
- [x] **T4.1**: Run `basedpyright autoBMAD/docuswarm/nodes/loader.py` to verify no type errors
- [x] **T4.2**: Run `basedpyright autoBMAD/docuswarm/` to verify no downstream type errors
- [x] **T4.3**: Verify NodeLoader still works correctly with simplified NodeConfig

## Dev Notes

### Technical Considerations
- **Backward Compatibility**: This is an internal refactoring - no external API changes
- **Data Flow**: The `questions` and `dependencies` fields were previously used for dynamic node configuration but are now managed by the graph.py edge definitions
- **Type Safety**: Must ensure NodeConfig remains a valid dataclass with all required fields

### Dependencies
- This story depends on EPIC-21 (NodeLoader Config Refactor) being complete
- No external services or new dependencies required
- Follows Ralph's principles: DRY, KISS, YAGNI

### Risk Mitigation
- **Risk**: Accidentally removing fields that are still in use
- **Mitigation**: Run comprehensive grep to verify no references exist before committing
- **Risk**: Breaking type checking
- **Mitigation**: Run basedpyright immediately after changes

## Testing

### Unit Tests
- [x] **UT1**: Verify NodeConfig can be instantiated with required fields only
- [x] **UT2**: Verify NodeConfig has expected fields: name, role, prompt, model, temperature, max_tokens
- [x] **UT3**: Verify removed dataclasses are not importable from loader.py

### Integration Tests
- [x] **IT1**: Verify NodeLoader can load node.yaml files without questions/dependencies fields
- [x] **IT2**: Verify pipeline graph construction still works correctly
- [x] **IT3**: Verify all node types (analyst, pm, ux, architect, po) load successfully

### Manual Testing
- [x] **MT1**: Run `python -c "from docuswarm.nodes.loader import NodeLoader; nl = NodeLoader(); print(nl.load_all())"` - should output node configs without errors
- [x] **MT2**: Run the existing test suite to ensure no regressions

---

## Dev Agent Record

### Implementation Summary
- Removed deprecated dataclasses from `autoBMAD/nodes/loader.py`:
  - `NodeQuestionConfig`
  - `NodeQuestionsConfig`
  - `NodeDependenciesConfig`
- Updated `autoBMAD/nodes/__init__.py` to remove exports of deprecated classes
- Verified with basedpyright: 0 errors (only 1 warning unrelated to changes)
- All 638 tests pass with no regressions
- loader.py coverage: 99%

### Files Modified
- `autoBMAD/nodes/loader.py` - Removed 3 deprecated dataclasses
- `autoBMAD/nodes/__init__.py` - Removed exports of deprecated classes

### Agent Model Used
- Claude Opus 4.6

---
*This story template was created by SM Agent and awaits SDK filling.*
