# Story 22.8: 创建 ux Persona 配置

## Status
**Status**: Ready for Development

**Last Updated**: 2026-03-02 22:25:47

## Story
**As a** developer,
**I want** to create the BMM-aligned persona.json for the ux node,
**So that** Sally (UX Designer) has complete role context for generating UX design deliverables.

## Acceptance Criteria

### Must Have (MoSCoW)
- [ ] Create `nodes/ux/persona.json` file with complete BMM-aligned persona configuration
- [ ] Include `name` field set to "Sally"
- [ ] Include `role` field: "User Experience Designer + UI Specialist"
- [ ] Include `identity` field with senior UX Designer description
- [ ] Include `communication_style` field describing the empathetic, storytelling approach
- [ ] Include `expertise` array with at least 6 items covering UX design competencies
- [ ] Include `principles` array with at least 5 guiding principles
- [ ] Include `output_format` object with type "ux-design" and format "markdown"
- [ ] Verify JSON file is valid and parseable

### Should Have
- [ ] Ensure persona.json follows the same structure as other node persona files
- [ ] Validate persona can be loaded by PersonaLoader without errors

### Could Have
- [ ] Add comments explaining key fields for future maintainers

## Tasks / Subtasks

### Task 1: Create nodes/ux directory structure
- [ ] Verify `nodes/ux/` directory exists
- [ ] If not exists, create directory structure following existing node patterns

### Task 2: Create persona.json file
- [ ] Create `nodes/ux/persona.json` with complete configuration
- [ ] Include all required fields per EPIC-22 specification
- [ ] Validate JSON syntax

### Task 3: Validate persona configuration
- [ ] Run JSON validation: `python -c "import json; json.load(open('nodes/ux/persona.json'))"`
- [ ] Compare structure with existing persona.json files (analyst, pm, architect, po)

### Task 4: Run PersonaLoader integration test
- [ ] Ensure PersonaLoader can successfully load the new persona.json
- [ ] Verify all fields are correctly parsed

## Dev Notes

### Technical Considerations
- The persona.json must follow the exact structure defined in the Persona dataclass in `autoBMAD/docuswarm/agents/persona.py`
- The `communication_style` field is a new addition per EPIC-22 - ensure backward compatibility with old loader
- File path should be `nodes/ux/persona.json` relative to project root
- Use double quotes for JSON strings (no single quotes)
- Ensure all arrays have at least one item

### Architecture Context
- This persona will be used by the IndependentAgent when executing UX design tasks
- The persona.json is pre-processed from `_bmad/bmm/agents/*.md` <persona> blocks
- Runtime has zero external file dependencies - all content embedded at build time

### Dependencies
- Depends on: Story 22.1 (Persona dataclass extension with communication_style)
- Depends on: Story 22.2 (_format_persona_section method implementation)
- Blocks: EPIC-23 (deprecated code removal), EPIC-24 (integration testing)

## Testing

### Unit Tests
- [ ] Test that persona.json is valid JSON
- [ ] Test that all required fields are present
- [ ] Test that expertise array has at least 6 items
- [ ] Test that principles array has at least 5 items
- [ ] Test that output_format has correct type and format

### Integration Tests
- [ ] Test PersonaLoader.load() with nodes/ux/persona.json
- [ ] Test that communication_style is correctly parsed
- [ ] Test backward compatibility with old persona loader

### Manual Testing
- [ ] Verify persona.json file exists at correct path
- [ ] Open file and verify content matches specification
- [ ] Run full persona test suite: `pytest tests/nodes/test_five_node_personas.py -v`

---

*This story was created by SM Agent following create-next-story procedure.*
*Epic: EPIC-22 - Persona 角色上下文与 System Prompt 重构*
