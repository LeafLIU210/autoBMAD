# Story 22.2: 实现 _format_persona_section()

## Status
**Status**: Ready for Done

**Last Updated**: 2026-03-02 22:27:15

## Story
**As a** developer,
**I want** to implement a method to format the persona section of system prompt,
**So that** it displays BMM-aligned role context.

## Acceptance Criteria
- [x] Given a Persona instance with all fields populated, when I call _format_persona_section(persona), then it returns a formatted string containing "# Persona: {name}" header
- [x] Given a Persona instance with all fields populated, when I call _format_persona_section(persona), then it returns a formatted string containing "**Role**: {role}" line
- [x] Given a Persona instance with all fields populated, when I call _format_persona_section(persona), then it returns a formatted string containing "## Identity" section with identity text
- [x] Given a Persona instance with all fields populated with communication_style set, when I call _format_persona_section(persona), then it returns a formatted string containing "## Communication Style" section with the style text
- [x] Given a Persona instance with all fields populated, when I call _format_persona_section(persona), then it returns a formatted string containing "## Expertise" section with bullet list
- [x] Given a Persona instance with all fields populated, when I call _format_persona_section(persona), then it returns a formatted string containing "## Guiding Principles" section with bullet list
- [x] Given a Persona without communication_style (empty string), when the section is formatted, then the Communication Style section is omitted entirely from the output
- [x] The method should be implemented as a static method in the IndependentAgent class
- [x] The method should handle empty expertise and principles lists gracefully

## Tasks / Subtasks
- [x] Task 1: Add _format_persona_section() static method to IndependentAgent class
- [x] Task 2: Implement expertise list formatting with bullet points
- [x] Task 3: Implement principles list formatting with bullet points
- [x] Task 4: Implement conditional communication_style section
- [x] Task 5: Handle edge cases (empty lists, missing optional fields)
- [x] Task 6: Write unit tests for the method

## Dev Notes
- The method should be a @staticmethod to align with the Epic technical specification
- Must import Persona type from the persona module
- Communication style section should only appear when communication_style is not empty (truthy)
- Each expertise item should be formatted as "- {item}" on its own line
- Each principle should be formatted as "- {principle}" on its own line
- The method should return a complete markdown-formatted string ready for system prompt injection
- Follow existing code style conventions in the codebase
- Ensure the output format matches the technical details example exactly

## Testing
### Unit Tests
- [x] Test with fully populated Persona object (all fields present)
- [x] Test with Persona having empty communication_style (should omit Communication Style section)
- [x] Test with Persona having empty expertise list (should show empty bullet list)
- [x] Test with Persona having empty principles list (should show empty bullet list)
- [x] Test that output contains correct header format "# Persona: {name}"
- [x] Test that output contains correct role format "**Role**: {role}"
- [x] Test that Identity section is properly formatted
- [x] Test that Expertise section contains all items as bullet points
- [x] Test that Guiding Principles section contains all items as bullet points
- [x] Test method signature accepts Persona type parameter

### Integration Tests
- [x] Integrate with _format_system_prompt() to verify persona section is included in final prompt
- [x] Verify backward compatibility with existing Persona objects without communication_style field

### Manual Testing
- [x] Run the method with Mary (Analyst) persona.json and verify output
- [x] Verify generated system prompt is valid markdown
- [x] Test with each of the five node personas (analyst, pm, ux, architect, po)

---
*This story template was created by SM Agent and awaits SDK filling.*
