# Story 34.4: Verify and Update Architect node.yaml Tools Config

## Status

**Status**: Ready for Done

**Last Updated**: 2026-04-07 10:33:12

## Story
**As a** system administrator,  
**I want** the Architect node to have complete and verified tool permissions configuration,  
**So that** the Architect agent can effectively read documents and search the docs/ directory to create comprehensive architecture documents.

## Acceptance Criteria
- [ ] `autoBMAD/nodes/architect/node.yaml` contains a complete `tools` section with `allowed_builtin_tools`, `file_permissions`, and `search_permissions`
- [ ] YAML validation passes without errors or warnings
- [ ] The Architect agent can successfully read files from the `docs/` directory and its subdirectories
- [ ] The Architect agent can use `Read` and `Glob` builtin tools as configured
- [ ] Configuration follows the unified format consistent with other nodes (analyst, pm, ux, po)

## Tasks / Subtasks
- [ ] Task 1: Verify current `tools` section in `autoBMAD/nodes/architect/node.yaml`
  - [ ] Confirm `allowed_builtin_tools` includes ["Read", "Glob"]
  - [ ] Confirm `file_permissions.allowed_read_dirs` includes ["docs/"]
  - [ ] Confirm `search_permissions.search_dirs` includes ["docs/"]
- [ ] Task 2: Run YAML syntax validation on the node.yaml file
  - [ ] Use `python -c "import yaml; yaml.safe_load(open('node.yaml'))"` to validate
  - [ ] Check for any indentation or formatting issues
- [ ] Task 3: Verify NodeLoader correctly parses the configuration
  - [ ] Run unit tests for NodeLoader if available
  - [ ] Manually verify through integration test or debug logging
- [ ] Task 4: Integration test - verify Architect agent can access docs/
  - [ ] Test reading a file in `docs/research/` subdirectory
  - [ ] Test glob search within `docs/` directory

## Dev Notes
- **Current State**: The `autoBMAD/nodes/architect/node.yaml` already contains a complete `tools` section as of previous Story 29.3 implementation
- **Expected Configuration**:
  ```yaml
  tools:
    allowed_builtin_tools: ["Read", "Glob"]
    file_permissions:
      allowed_read_dirs:
        - "docs/"
    search_permissions:
      search_dirs:
        - "docs/"
  ```
- **PathValidator Behavior**: The root `docs/` entry covers all subdirectories (e.g., `docs/research/`, `docs/architecture/`) through prefix matching
- **Security**: PathValidator provides multi-layer defense (directory whitelist, symlink resolution, file type restrictions) - no additional security changes needed
- **EvaluatorAgent**: Remains tool-free by design - no changes needed for this node
- **Dependencies**: This story depends on EPIC-31 (skills config section) and EPIC-32 (node.yaml structure)

## Testing
### Unit Tests
- [ ] Test case 1: Validate YAML syntax using PyYAML parser
- [ ] Test case 2: Verify NodeLoader correctly extracts `allowed_builtin_tools` list
- [ ] Test case 3: Verify NodeLoader correctly extracts `file_permissions.allowed_read_dirs`
- [ ] Test case 4: Verify NodeLoader correctly extracts `search_permissions.search_dirs`

### Integration Tests
- [ ] Integration test 1: Architect agent can execute `read_document` tool on a file in `docs/`
- [ ] Integration test 2: Architect agent can execute `glob_search` tool to find files in `docs/research/`
- [ ] Integration test 3: Architect agent can read nested subdirectories (e.g., `docs/architecture/design-patterns/`)

### Manual Testing
- [ ] Manual test 1: Run `python -c "import yaml; print(yaml.safe_load(open('autoBMAD/nodes/architect/node.yaml')))"` and verify tools section parses correctly
- [ ] Manual test 2: Start the DocuSwarm system and verify Architect node initializes without tool configuration errors
- [ ] Manual test 3: Review startup logs to confirm tools are loaded for Architect node

---
*Story 34.4 - EPIC-34: Tool Permissions Full Open Access*
