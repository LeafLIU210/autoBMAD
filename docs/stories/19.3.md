# Story 19.3: SessionManager Tests Update

## Status

**Status**: Ready for Done

**Last Updated**: 2026-03-02 10:47:20

## Story
**As a** developer,
**I want to** update SessionManager tests to use standard Python mocks instead of Kimi SDK mocks,
**So that** the tests don't depend on the deprecated Kimi SDK mock and can be maintained more easily.

## Acceptance Criteria
- [x] 移除 `kimi_agent_sdk` 模块的导入和 mock 依赖
- [x] 使用 `AsyncMock` 替代 `Session` 和 `MessageAggregator` 的 mock
- [x] 使用 `mock_sdk_wrapper` fixture 提供测试数据
- [x] 验证 `single_prompt` 方法返回 dict 格式的消息列表
- [x] 验证所有现有测试用例仍然通过
- [x] 确保测试覆盖率不低于 80%

## Tasks / Subtasks
- [x] Task 1: 创建 `mock_sdk_wrapper` fixture (如果 conftest.py 中尚不存在)
- [x] Task 2: 重写 `test_single_prompt` 测试方法，使用新的 mock 方式
- [x] Task 3: 重写 `test_create_session` 测试方法
- [x] Task 4: 重写 `test_get_messages` 测试方法
- [x] Task 5: 移除所有 `patch("autoBMAD.docuswarm.llm.session_manager.Session")` 调用
- [x] Task 6: 移除所有 `patch("autoBMAD.docuswarm.llm.session_manager.MessageAggregator")` 调用
- [x] Task 7: 运行完整测试套件验证所有测试通过

## Dev Notes
- KimiSessionManager 类已重命名为 SessionManager，需更新导入路径
- 新的 `mock_sdk_wrapper` fixture 应返回包含 `execute` 方法的 AsyncMock
- 返回格式必须为 dict 列表：`[{"role": "assistant", "content": "..."}]`
- 需确保测试使用 `pytest.mark.asyncio` 装饰器
- 如果 conftest.py 中没有 `mock_sdk_wrapper`，需要在其中添加该 fixture

### 关键代码变更

```python
# 旧代码 (需移除)
from unittest.mock import patch, MagicMock
from kimi_agent_sdk import Session, MessageAggregator

# 新代码 (需使用)
from unittest.mock import AsyncMock
import pytest

@pytest.fixture
def mock_sdk_wrapper():
    wrapper = AsyncMock()
    wrapper.execute.return_value = MockSDKResult(
        success=True,
        content="Mock response",
        messages=[]
    )
    return wrapper
```

### 依赖项
- 依赖于 Story 19.1 (Conftest.py Refactoring) 完成的 fixtures
- 依赖于 Story 19.2 (Mock Data Classes Creation) 的 `MockSDKResult` 类

## Testing

### Unit Tests
- [x] `test_single_prompt_returns_dict_list`: 验证 single_prompt 返回 dict 格式列表
- [x] `test_create_session_initializes_sdk`: 验证 SessionManager 正确初始化 SDK wrapper
- [x] `test_get_messages_returns_dicts`: 验证 get_messages 返回 dict 格式
- [x] `test_error_handling`: 验证 SDK 执行失败时的错误处理

### Integration Tests
- [x] Integration test: 运行 `pytest tests/unit/test_session_manager.py -v` 确认所有测试通过
- [x] Integration test: 运行 `pytest tests/unit/ -v` 确认没有引入新的失败

### Manual Testing
- [x] Manual test: 确认测试文件可以正常导入所有依赖
- [x] Manual test: 确认 conftest.py 的 fixtures 可以被正确加载

## File List
- tests/conftest.py - Added `mock_sdk_wrapper` fixture
- tests/unit/test_session_manager_dict_migration.py - Updated test file to use AsyncMock and conftest fixture

---

*Story created by SM Agent for Epic 19: Test Dependency Migration*
