# Story 13.6: Orchestrator 集成

## Status
**Status**: Ready for Done
**Last Updated**: 2026-03-02 01:36:07

## Story
**As a** developer,
**I want to** integrate ContextValidator into the HybridOrchestrator,
**So that** the validation flow uses the new standalone component instead of the embedded `_validate_context` method.

## Acceptance Criteria
- [x] Orchestrator 中移除 `_validate_context` 方法（原方法约 80 行代码）
- [x] 添加 `_context_validator` 实例变量初始化为 `None`
- [x] 添加 `_get_context_validator` 辅助方法实现懒加载
- [x] `start_pipeline` 使用新的 `validator.validate()` 调用方式
- [x] 支持配置 `fail_open` 模式（默认 `False`）
- [x] 验证失败时抛出 `ContextValidationError` 异常
- [x] 当 `fallback_used=True` 时记录 warning 日志
- [x] 保持原有 `start_pipeline` 的返回类型和接口兼容

## Tasks / Subtasks
- [x] Task 1: 定位并删除 `orchestrator.py` 中的 `_validate_context` 方法
- [x] Task 2: 在 `HybridOrchestrator.__init__` 中添加 `self._context_validator: ContextValidator | None = None`
- [x] Task 3: 实现 `_get_context_validator` 方法（懒加载初始化）
- [x] Task 4: 修改 `start_pipeline` 方法，用新验证流程替换原有调用
- [x] Task 5: 导入 `ContextValidator` 和 `ContextValidationError`
- [x] Task 6: 确保 `fail_open` 配置可从外部传入
- [x] Task 7: 运行 basedpyright 确保类型检查通过
- [x] Task 8: 运行 ruff 确保代码风格符合规范

## Dev Notes
- **依赖组件**: 需要先完成 Story 13.1-13.5，确保 `ContextValidator` 和 `ValidationResult` 已实现
- **向后兼容**: 保持 `start_pipeline` 的返回类型不变，验证失败时抛出异常的处理方式与原逻辑一致
- **懒加载**: 使用 `_get_context_validator` 实现延迟初始化，避免循环导入
- **日志处理**: 使用项目现有的日志结构，`fallback_used` 时记录 warning 级别日志
- **错误传播**: 验证失败时抛出的 `ContextValidationError` 需要在调用层正确捕获
- **测试覆盖**: 需要覆盖集成后的端到端验证流程，包括 fail_open=True 和 fail_open=False 两种模式

## Testing

### Unit Tests
- [x] Test: `_get_context_validator` returns same instance on multiple calls (singleton behavior)
- [x] Test: `start_pipeline` calls `validator.validate()` with correct context
- [x] Test: Validation failure raises `ContextValidationError` when `fail_open=False`
- [x] Test: Validation failure returns valid result with `fallback_used=True` when `fail_open=True`
- [x] Test: Original `_validate_context` method is removed

### Integration Tests
- [x] Integration: Full pipeline execution with valid context succeeds
- [x] Integration: Full pipeline execution with invalid context fails appropriately
- [x] Integration: Verify orchestrator line count reduced by ~60 lines

### Manual Testing
- [x] Manual: Run existing integration tests to ensure no regression
- [x] Manual: Verify error messages are clear and actionable for developers
- [x] Manual: Test with fail_open=True mode in development environment

## Implementation Summary
- Removed `_validate_context` method (~83 lines) and `CONTEXT_VALIDATION_PROMPT` constant (~25 lines) from orchestrator.py
- Added `_context_validator` attribute and `_get_context_validator` lazy loading method
- Added `fail_open` parameter to `HybridOrchestrator.__init__`
- Updated `start_pipeline` to use `validator.validate()` instead of `_validate_context`
- Added proper warning log when `fallback_used=True`
- All 135 unit tests pass with 30% overall coverage

---

## QA Results

**Gate Status**: PASS
**Reviewed by**: Quinn (Test Architect)
**Date**: 2026-03-02

### Verification Summary

| Check | Status | Notes |
|-------|--------|-------|
| BasedPyright | ✅ PASS | 0 errors, 0 warnings |
| Ruff Linting | ✅ PASS | All checks passed |
| Unit Tests | ✅ PASS | 27 tests passed (67% coverage on orchestrator.py) |

### Implementation Verification
- ✅ Old `_validate_context` method removed (~83 lines)
- ✅ `_context_validator` lazy loading attribute added
- ✅ `_get_context_validator()` method implemented with singleton behavior
- ✅ `start_pipeline` uses `validator.validate()` pattern
- ✅ `fail_open` parameter added to `__init__`
- ✅ `ContextValidationError` raised on validation failure
- ✅ Warning logged when `fallback_used=True`

All 8 acceptance criteria verified. Story complete.

---
*This story was created by SM Agent (Bob) for EPIC-13 ContextValidator extraction refactoring.*
