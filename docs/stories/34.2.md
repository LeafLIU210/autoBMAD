# Story 34.2: Verify and Update PM node.yaml Tools Config

## Status

**Status**: Ready for Done

**Last Updated**: 2026-04-07 10:34:11

## Story
**As a** DocuSwarm system administrator,
**I want** the PM (Product Manager) node to have complete tools configuration in its node.yaml file,
**So that** the PM agent can access read and search tools to gather context from the docs/ directory when creating product requirements and managing epics.

## Acceptance Criteria
- [ ] `autoBMAD/nodes/pm/node.yaml` has a complete `tools` section with `allowed_builtin_tools`, `file_permissions`, and `search_permissions`
- [ ] YAML validation passes without errors or warnings for the PM node.yaml file
- [ ] The PM agent can successfully read files from the `docs/` directory and its subdirectories
- [ ] The tools configuration follows the unified format used across all nodes (Read, Glob tools with docs/ access)
- [ ] NodeLoader correctly parses the PM node configuration at system startup

## Tasks / Subtasks
- [ ] Task 1: Read and verify current `autoBMAD/nodes/pm/node.yaml` configuration
- [ ] Task 2: Ensure `tools.allowed_builtin_tools` includes ["Read", "Glob"]
- [ ] Task 3: Ensure `tools.file_permissions.allowed_read_dirs` includes ["docs/"]
- [ ] Task 4: Ensure `tools.search_permissions.search_dirs` includes ["docs/"]
- [ ] Task 5: Run YAML validation on the modified file
- [ ] Task 6: Verify NodeLoader correctly loads the PM node configuration
- [ ] Task 7: Update any related documentation if needed

## Dev Notes
- **File Location**: `autoBMAD/nodes/pm/node.yaml` (NOT the deprecated `nodes/pm/node.yaml`)
- **Current Status**: According to TD-002 analysis, the PM node configuration may already be complete - this story is primarily for verification
- **Unified Configuration Pattern**:
  ```yaml
  tools:
    allowed_builtin_tools: ["Read", "Glob"]
    file_permissions:
      allowed_read_dirs:
        - "docs/"
    search_permissions:
      search_dirs:
        - "docs/"
  ```
- **Security**: PathValidator provides multi-layer defense (directory whitelist, symlink resolution, file type restrictions)
- **Dependencies**: None - this is a verification task that can be done independently
- **Verification Method**: Check that the PM agent can list documents and read files from docs/research/ subdirectory

## Testing
### Unit Tests
- [ ] Test that NodeLoader correctly parses PM node.yaml without errors
- [ ] Test that tools configuration is properly validated against schema
- [ ] Test that file_permissions are correctly extracted into the node configuration

### Integration Tests
- [ ] Integration test: PM agent can execute read_document tool on a file in docs/
- [ ] Integration test: PM agent can execute list_documents tool in docs/ directory
- [ ] Integration test: PM agent can execute glob_search tool to find files in docs/
- [ ] Integration test: PM agent can read files from docs/research/ subdirectory

### Manual Testing
- [ ] Start the DocuSwarm system and verify PM node loads without configuration errors
- [ ] Check startup logs for PM node tools configuration
- [ ] Manually verify PM agent can access docs/ directory contents

---
*This story was prepared by the Scrum Master Agent based on EPIC-34 requirements.*
