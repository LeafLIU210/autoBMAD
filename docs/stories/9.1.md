# Story 9.1: Session Persistence Strategy

## Status

**Status**: Ready for Development

**Last Updated**: 2026-02-21 16:48:16

## Story
**As a** developer,
**I want to** implement hierarchical session ID naming,
**So that** sessions can be uniquely identified and resumed at any level.

## Acceptance Criteria
- [x] Session ID generator follows naming convention: `docuswarm-{pipeline_id}-{node_id}[-iter{n}]`
- [x] Pipeline-level session ID format: `docuswarm-{pipeline_id}`
- [x] Node-level session ID format: `docuswarm-{pipeline_id}-{node_id}`
- [x] Iteration-level session ID format: `docuswarm-{pipeline_id}-{node_id}-iter{n}`
- [x] Session IDs are deterministic (same inputs always produce same output)
- [x] Session IDs stored in LangGraph PipelineState for recovery
- [x] Utility function handles None values for optional parameters

## Tasks / Subtasks
- [x] Task 1: Create session ID generation utility in `docuswarm/utils/session_ids.py`
- [x] Task 2: Implement `generate_session_id()` function with pipeline/node/iteration levels
- [x] Task 3: Add type hints and docstrings to utility function
- [x] Task 4: Integrate session ID generation into PipelineState schema
- [x] Task 5: Write unit tests for all three ID levels (pipeline, node, iteration)
- [x] Task 6: Test edge cases (empty strings, special characters in IDs)

## Dev Notes
- The session ID generator should be a pure function with no side effects
- Use string joining with "-" separator for consistent formatting
- PipelineState schema needs a new field `session_ids: dict` to store hierarchical IDs
- Consider adding validation to ensure pipeline_id and node_id don't contain hyphens
- The utility should be importable from `docuswarm.utils.session_ids`
- Follow existing code style: use `loguru` for logging, `pydantic` for type hints

## Testing

### Unit Tests
- [x] Test pipeline-level ID: `generate_session_id("pipeline-001")` returns `"docuswarm-pipeline-001"`
- [x] Test node-level ID: `generate_session_id("pipeline-001", "node-a")` returns `"docuswarm-pipeline-001-node-a"`
- [x] Test iteration-level ID: `generate_session_id("pipeline-001", "node-a", 3)` returns `"docuswarm-pipeline-001-node-a-iter3"`
- [x] Test None node_id: `generate_session_id("pipeline-001", None)` returns `"docuswarm-pipeline-001"`
- [x] Test None iteration: `generate_session_id("pipeline-001", "node-a", None)` returns `"docuswarm-pipeline-001-node-a"`
- [x] Test determinism: multiple calls with same args return identical IDs

### Integration Tests
- [x] Integration test: Verify session IDs are stored in PipelineState during pipeline creation
- [x] Integration test: Verify session IDs can be retrieved for recovery

### Manual Testing
- [ ] Manual test: Run pipeline and verify session IDs appear in logs/state

---

## File List
- `docuswarm/utils/session_ids.py` (NEW) - Session ID generation utility
- `tests/unit/test_session_ids.py` (NEW) - Unit tests for session IDs
- `docuswarm/pipeline/state.py` (MODIFIED) - Added session_ids field to PipelineState, update_node_session_id(), get_session_id()
- `tests/unit/test_pipeline_state.py` (MODIFIED) - Added tests for session ID integration

---

**Definition of Done**:
- Session IDs unique and deterministic
- All three levels generate correct format
- IDs stored in pipeline state
- Unit tests pass

---

## QA Results

**Gate Decision**: PASS

**Review Summary**: All acceptance criteria verified and met. Implementation complete with comprehensive test coverage.

**Tool Results**:
- BasedPyright (Type Checking): PASS (0 errors, 4 deprecation warnings)
- Ruff (Linting): PASS (auto-fixed 3 issues)
- pytest: PASS (78 tests passed - 25 session_ids + 53 pipeline_state)

**Quality Score**: 100/100

---

## Change Log
- 2026-02-21: Implemented session ID generation utility with hierarchical naming
- 2026-02-21: Integrated session IDs into PipelineState schema
- 2026-02-21: Added comprehensive unit tests (78 tests, 100% pass)
- 2026-02-21: Updated story status to Ready for Review

*Story prepared by SM Agent for SDK implementation.*
