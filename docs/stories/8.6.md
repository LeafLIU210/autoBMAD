# Story 8.6: Tool Auto-Dispatch Integration Test

## Status
**Status**: Ready for Development
**Last Updated**: 2026-02-21 12:06:41

## Story
**As a** developer,
**I want** to verify that the SDK correctly auto-dispatches tool calls,
**So that** I have confidence the tool migration is complete and correct.

## Acceptance Criteria
- [ ] Integration test creates session with agent file
- [ ] Prompt triggers tool call (e.g., "Create a deliverable titled 'Test'")
- [ ] SDK auto-dispatches to CreateDeliverableTool
- [ ] Tool receives correctly deserialized Pydantic params
- [ ] ToolOk result returned to SDK
- [ ] SDK includes tool result in conversation flow

## Tasks / Subtasks
- [ ] Task 1: Create integration test file `tests/integration/test_tool_dispatch.py`
- [ ] Task 2: Set up mock output handler and context store with appropriate fixtures
- [ ] Task 3: Create session with agent file pointing to CreateDeliverableTool and UpdateContextTool
- [ ] Task 4: Send prompt that triggers tool call (e.g., "Create a deliverable titled 'Test Doc' with content 'Hello World'")
- [ ] Task 5: Verify tool was called with correct Pydantic-deserialized parameters
- [ ] Task 6: Verify ToolOk result was returned and included in conversation
- [ ] Task 7: Add test case for invalid parameters to verify ToolError is returned
- [ ] Task 8: Add test case for UpdateContextTool to verify both tools work

## Dev Notes
- The integration test requires the kimi-agent-sdk to be properly configured with agent file support
- Use pytest-asyncio for async test execution
- Mock the output handler to verify deliverable creation without filesystem side effects
- The agent file path should be `docuswarm/agents/configs/independent_agent.yaml`
- Verify that SDK auto-dispatch calls the tool's `__call__` method directly with deserialized Pydantic params
- Consider using VCR or similar for recording/replaying API calls to avoid hitting rate limits during development
- The test should verify the complete flow: user prompt → LLM response with tool call → SDK auto-dispatch → tool execution → tool result → SDK result message

## Testing
### Unit Tests
- [ ] Test that mock output handler's `save_deliverable` is called with correct arguments
- [ ] Test that mock context store's `update` is called with correct arguments
- [ ] Test ToolOk return value contains expected output message
- [ ] Test ToolError return value for invalid parameters

### Integration Tests
- [ ] Integration test: CreateDeliverableTool is invoked via SDK auto-dispatch
- [ ] Integration test: UpdateContextTool is invoked via SDK auto-dispatch
- [ ] Integration test: Invalid parameters produce ToolError response
- [ ] Integration test: Tool results are included in conversation flow

### Manual Testing
- [ ] Manual test: Run integration test with real Kimi API (if API key available)
- [ ] Manual test: Verify tool results appear in conversation aggregator output

---
*This story template was created by SM Agent and awaits SDK filling.*
