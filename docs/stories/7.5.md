# Story 7.5: EvaluatorAgent Adaptation

## Status
**Status**: Ready for Done

**Last Updated**: 2026-02-21 10:19:14

## Story
**As a** developer
**I want to** update EvaluatorAgent to use `prompt()` high-level API with thinking mode
**So that** evaluations leverage SDK's simplified single-shot API

## Acceptance Criteria
- [x] `_call_llm()` uses `session_manager.single_prompt()` with mode="thinking"
- [x] Context isolation checks preserved (ContextFilter)
- [x] Evaluation prompt formatting unchanged
- [x] Response parsing adapted to SDK `Message` type
- [x] No tool calling (evaluator is read-only)
- [x] `yolo=True` for evaluator (no tools to approve)

## Tasks / Subtasks
- [x] Task 1: Modify `docuswarm/agents/evaluator.py` - Replace `self.llm` with `self.session_manager` in constructor
- [x] Task 2: Update `_call_llm()` method to use `session_manager.single_prompt()` instead of `KimiClient.chat()`
- [x] Task 3: Adapt response parsing from `ChatResponse` to `list[Message]` type
- [x] Task 4: Preserve context isolation validation logic (ContextFilter checks)
- [x] Task 5: Update evaluation prompt formatting to work with single_prompt API
- [x] Task 6: Set `yolo=True` parameter (evaluator has no tools to approve)
- [x] Task 7: Update type hints and imports for new SDK types
- [x] Task 8: Run unit tests and fix any breaking changes
- [x] Task 9: Run integration test to verify end-to-end evaluation flow

## Dev Notes
- The EvaluatorAgent is read-only and does not require tool execution, so `yolo=True` is safe to use
- The `single_prompt()` API returns `list[Message]` instead of `ChatResponse` - need to update `_parse_evaluation()` method
- Context isolation must remain intact - ensure ContextFilter validation is called before LLM invocation
- The thinking mode should be enabled via `mode="thinking"` parameter to single_prompt()
- May need to update the prompt formatting logic since single_prompt takes a single string rather than message history
- Consider backward compatibility during transition period - keep KimiClient path available but commented
- Structured logging should be preserved using `structlog`
- SDK exception handling should be implemented (RunCancelled, ChatProviderError, etc.)

## Implementation Summary
- Updated `_call_llm()` to use `session_manager.single_prompt()` with `mode="thinking"` and `yolo=True`
- Maintained backward compatibility with legacy `llm_client` path for existing tests
- Updated `_parse_response()` to handle both `list[Message]` (SDK) and `ChatResponse` (legacy) types
- Added proper handling for Message content that can be string or list of ContentPart objects
- Added 6 new unit tests for SDK adaptation covering:
  - Initialization with session_manager
  - single_prompt() call with correct parameters
  - Prompt formatting
  - SDK exception handling
  - Message response parsing
  - Context isolation preservation

## Testing Results
- **Unit Tests**: 45 tests passing (80% coverage on evaluator.py)
- **Integration Tests**: 12 tests passing
- **Total**: 57 tests passing

## File Changes
- Modified: `docuswarm/agents/evaluator.py`
- Modified: `tests/unit/test_evaluator_agent.py` (added SDK tests)

---
*Story implemented by James (Dev Agent)*
