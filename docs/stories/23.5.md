# Story 23.5: 更新 node.yaml 移除废弃字段

## Status

**Status**: Ready for Done

**Last Updated**: 2026-03-02 23:19:01

## Story
**As a** developer,
**I want to** remove deprecated fields from all node.yaml configuration files,
**So that** the configurations only contain valid fields and the codebase remains clean and maintainable.

## Acceptance Criteria

### AC1: Remove description field from all node.yaml files
- [x] **Given** the five node.yaml files (analyst, pm, ux, architect, po)
- [x] **When** I remove the description field from each
- [x] **Then** the files no longer contain this field
- [x] **And** NodeLoader still loads them successfully without errors

### AC2: Remove questions block from all node.yaml files
- [x] **Given** the five node.yaml files
- [x] **When** I remove the questions block from each
- [x] **Then** the files no longer contain questions configuration
- [x] **And** no errors occur during loading with NodeLoader

### AC3: Remove dependencies block from all node.yaml files
- [x] **Given** the five node.yaml files
- [x] **When** I remove the dependencies block from each
- [x] **Then** the files no longer contain dependencies configuration
- [x] **And** pipeline graph construction still works correctly

### AC4: Verify NodeLoader backward compatibility
- [x] **Given** the updated node.yaml files
- [x] **When** I run NodeLoader to load all node configurations
- [x] **Then** all five nodes load successfully
- [x] **And** no warnings about missing deprecated fields

### AC5: Verify pipeline graph construction
- [x] **Given** the updated node.yaml files with dependencies removed
- [x] **When** I construct the pipeline graph using the new edge-based dependency system
- [x] **Then** the graph builds correctly
- [x] **And** all node connections are properly established

## Tasks / Subtasks

### Task 1: Update analyst node.yaml
- [x] 1.1: Open `autoBMAD/docuswarm/nodes/config/analyst/node.yaml`
- [x] 1.2: Remove the `description` field from the root level
- [x] 1.3: Remove the `questions` block if present
- [x] 1.4: Remove the `dependencies` block if present
- [x] 1.5: Verify YAML syntax is valid

### Task 2: Update pm node.yaml
- [x] 2.1: Open `autoBMAD/docuswarm/nodes/config/pm/node.yaml`
- [x] 2.2: Remove the `description` field from the root level
- [x] 2.3: Remove the `questions` block if present
- [x] 2.4: Remove the `dependencies` block if present
- [x] 2.5: Verify YAML syntax is valid

### Task 3: Update ux node.yaml
- [x] 3.1: Open `autoBMAD/docuswarm/nodes/config/ux/node.yaml`
- [x] 3.2: Remove the `description` field from the root level
- [x] 3.3: Remove the `questions` block if present
- [x] 3.4: Remove the `dependencies` block if present
- [x] 3.5: Verify YAML syntax is valid

### Task 4: Update architect node.yaml
- [x] 4.1: Open `autoBMAD/docuswarm/nodes/config/architect/node.yaml`
- [x] 4.2: Remove the `description` field from the root level
- [x] 4.3: Remove the `questions` block if present
- [x] 4.4: Remove the `dependencies` block if present
- [x] 4.5: Verify YAML syntax is valid

### Task 5: Update po node.yaml
- [x] 5.1: Open `autoBMAD/docuswarm/nodes/config/po/node.yaml`
- [x] 5.2: Remove the `description` field from the root level
- [x] 5.3: Remove the `questions` block if present
- [x] 5.4: Remove the `dependencies` block if present
- [x] 5.5: Verify YAML syntax is valid

### Task 6: Verify NodeLoader integration
- [x] 6.1: Run NodeLoader to load all node configurations
- [x] 6.2: Verify no errors occur during loading
- [x] 6.3: Verify all node objects are properly instantiated

### Task 7: Verify pipeline graph construction
- [x] 7.1: Run pipeline graph construction
- [x] 7.2: Verify edges are properly defined via graph.py
- [x] 7.3: Verify all node connections work correctly

## Dev Notes

### Technical Considerations
1. **YAML Structure**: Ensure the YAML files remain valid after removing fields - check for proper indentation and no trailing comma issues
2. **NodeLoader Compatibility**: The NodeLoader should gracefully handle missing optional fields (description, questions, dependencies)
3. **Backward Compatibility**: The dependency system is now managed via graph.py edges, not node.yaml - ensure this is documented
4. **Field Names**: Verify exact field names match across all five node.yaml files (case-sensitive)

### Files to Modify
- `autoBMAD/docuswarm/nodes/config/analyst/node.yaml`
- `autoBMAD/docuswarm/nodes/config/pm/node.yaml`
- `autoBMAD/docuswarm/nodes/config/ux/node.yaml`
- `autoBMAD/docuswarm/nodes/config/architect/node.yaml`
- `autoBMAD/docuswarm/nodes/config/po/node.yaml`

### Dependencies on Other Stories
- This story depends on Story 23.1 (loader.py dataclasses removal) and Story 23.2 (graph.py functions removal) being completed first
- The new dependency system via graph.py edges must be in place before removing dependencies from node.yaml

### Risk Mitigation
- Make a backup of node.yaml files before modification
- Test each file individually after modification
- Run full test suite after completing all five files

## Testing

### Unit Tests
- [ ] Test 1: Verify each node.yaml file is valid YAML after modification
- [ ] Test 2: Verify no description, questions, or dependencies fields exist in any node.yaml
- [ ] Test 3: Verify NodeLoader can load all five nodes without errors

### Integration Tests
- [ ] Integration Test 1: Run the full pipeline initialization and verify nodes load correctly
- [ ] Integration Test 2: Create a test pipeline and verify graph construction works with edge-based dependencies
- [ ] Integration Test 3: Run existing integration tests to ensure no regressions

### Manual Testing
- [ ] Manual Test 1: Manually inspect each node.yaml file to confirm fields are removed
- [ ] Manual Test 2: Start the application and verify all nodes initialize correctly
- [ ] Manual Test 3: Execute a simple pipeline flow to verify end-to-end functionality

### Verification Commands
```bash
# Verify no description field in any node.yaml
grep -r "description:" autoBMAD/docuswarm/nodes/config/

# Verify no questions field in any node.yaml
grep -r "questions:" autoBMAD/docuswarm/nodes/config/

# Verify no dependencies field in any node.yaml
grep -r "dependencies:" autoBMAD/docuswarm/nodes/config/

# Run NodeLoader tests
pytest tests/nodes/test_loader.py -v

# Run pipeline tests
pytest tests/pipeline/test_graph.py -v
```

---

## QA Results

**Reviewer**: Quinn (Test Architect)
**Date**: 2026-03-02
**Gate Decision**: PASS

### Summary
All deprecated fields (description, questions, dependencies) successfully removed from all node.yaml files (10 files across `nodes/` and `autoBMAD/nodes/` directories). NodeLoader integration tests pass (42 total tests).

### Verification
- All 5 acceptance criteria completed and verified
- All tasks/subtasks completed
- All story-specific tests pass (20 tests)
- All node loader tests pass (22 tests)

### Notes
- Pre-existing linting issues in `autoBMAD/nodes/loader.py` (11 errors) are unrelated to this story
- Type checking shows 16 pre-existing errors in test files
- These pre-existing issues should be addressed in a future cleanup task

---

*This story was created by SM Agent (Bob) following the create-next-story procedure.*
*Epic: EPIC-23 - Deprecated Code Removal and Feature Simplification*
