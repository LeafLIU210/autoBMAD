# Story 19.2: Mock Data Classes Creation

## Status
**Status**: Ready for Done

**Last Updated**: 2026-03-02 10:47:27

## Story
**As a** developer,
**I want to** create simple mock data classes for testing,
**So that** tests can use them for data setup without depending on Kimi SDK.

## Acceptance Criteria
- [x] Create `MockSDKResult` dataclass with success, content, error, duration, messages, and tool_calls fields
- [x] Create `MockMessage` dataclass with role and content fields
- [x] MockMessage supports tool call extraction via `get_tool_calls()` method
- [x] MockSDKResult supports JSON serialization
- [x] Create predefined mock data constants (MOCK_DELIVERABLE_MESSAGE, MOCK_EVALUATION_RESPONSE)
- [x] All mock classes are importable from `tests.mocks` module

## Tasks / Subtasks
- [x] Task 1: Create `tests/mocks.py` file with dataclass definitions
- [x] Task 2: Implement `MockSDKResult` dataclass with default factory for lists
- [x] Task 3: Implement `MockMessage` dataclass with `get_tool_calls()` method
- [x] Task 4: Add predefined mock data constants (MOCK_DELIVERABLE_MESSAGE, MOCK_EVALUATION_RESPONSE)
- [x] Task 5: Verify imports work correctly from `tests.mocks`
- [x] Task 6: Run unit tests to verify mock classes work as expected

## Dev Notes
- All mock classes must be independent of kimi-agent-sdk
- Use Python dataclasses with field defaults for mutable defaults
- MockSDKResult should mirror the actual SDKResult class structure
- MockMessage should handle both string content and list content (with tool_use blocks)
- The get_tool_calls() method should extract tool_use blocks from content list
- Follow Ralph's principles: DRY, KISS, YAGNI

## Testing
### Unit Tests
- [x] Test MockSDKResult instantiation with default values
- [x] Test MockSDKResult with custom values
- [x] Test MockSDKResult JSON serialization
- [x] Test MockMessage with string content
- [x] Test MockMessage with list content (text + tool_use)
- [x] Test MockMessage.get_tool_calls() extracts correct data

### Integration Tests
- [ ] Verify mocks work with SessionManager tests
- [ ] Verify mocks work with IndependentAgent tests

### Manual Testing
- [x] Verify imports work: `from tests.mocks import MockSDKResult, MockMessage, MOCK_DELIVERABLE_MESSAGE`

---

*This story was created by SM Agent for Epic 19 (Test Dependency Migration).*
