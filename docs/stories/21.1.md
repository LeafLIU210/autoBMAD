# Story 21.1: 添加 NodeTaskConfig 数据类

## Status

**Status**: Ready for Done

**Last Updated**: 2026-03-02 21:27:54

## Story
**As a** developer,
**I want** to add the NodeTaskConfig dataclass with name, description, and role_supplement fields,
**So that** we can store task-specific configuration for each node in the BMM-aligned format.

## Acceptance Criteria
- [x] **AC1**: Given the loader.py file exists, when I add the NodeTaskConfig dataclass with name, description, and role_supplement fields, then the class can be instantiated with minimal fields (name, description), and the role_supplement field defaults to empty string.
- [x] **AC2**: Given a NodeTaskConfig instance, when I serialize it to dict, then all fields are properly exported.
- [x] **AC3**: Given a NodeTaskConfig instance with all fields populated, when I access each field, then the values are correctly stored and retrievable.
- [x] **AC4**: Given the dataclass is used in NodeConfig, when the task field is optional, then it can be set to None without errors.
- [x] **AC5**: Given the dataclass follows Python dataclass conventions, when used with typing hints, then type checking passes without errors.

## Tasks / Subtasks
- [x] **Task 1**: Add NodeTaskConfig dataclass to loader.py
  - [x] Import required dataclass decorators and typing
  - [x] Define the dataclass with name, description, role_supplement fields
  - [x] Add proper docstring following existing codebase patterns
- [x] **Task 2**: Add field defaults and type annotations
  - [x] Ensure name and description are required str fields
  - [x] Ensure role_supplement defaults to empty string
- [x] **Task 3**: Verify dataclass serialization
  - [x] Test asdict() export works correctly
  - [x] Test dataclass_to_dict utility if used elsewhere
- [x] **Task 4**: Update type hints in related modules
  - [x] Import NodeTaskConfig in __init__.py if needed
  - [x] Add to public API exports

## Dev Notes
- **Dataclass Design**: Follow the existing pattern in loader.py (NodeDeliverableConfig, NodeAgentConfig)
- **Backward Compatibility**: This is a net-new addition, no deprecated fields being removed in this story
- **Type Safety**: Use `str` type hints, avoid `Any` unless necessary
- **Documentation**: Include comprehensive docstring with Attributes section
- **BMM Alignment**: The role_supplement field is designed to hold pre-processed BMM workflow content from _bmad/bmm/
- **Naming Convention**: Follow existing dataclass naming pattern (PascalCase class, snake_case attributes)

## Testing

### Unit Tests
- [x] **Test 1**: test_minimal_task_config - Create NodeTaskConfig with only required fields (name, description), verify role_supplement defaults to ""
- [x] **Test 2**: test_full_task_config - Create NodeTaskConfig with all fields, verify all values are correctly stored
- [x] **Test 3**: test_task_config_serialization - Create instance and serialize to dict, verify all fields present in output
- [x] **Test 4**: test_task_config_field_access - Verify each field can be accessed independently
- [x] **Test 5**: test_task_config_equality - Verify two instances with same values are equal

### Integration Tests
- [x] **Test 6**: test_node_task_config_in_node_config - Verify NodeTaskConfig can be used as task field in NodeConfig
- [x] **Test 7**: test_task_config_optional_in_node_config - Verify NodeConfig.task can be None

### Manual Testing
- [x] **Manual 1**: Run basedpyright to verify type annotations are correct
- [x] **Manual 2**: Run ruff check to verify code style compliance

## Dev Agent Record

### Implementation Summary
- Added `NodeTaskConfig` dataclass to `nodes/loader.py` with:
  - `name: str` (required)
  - `description: str` (required)
  - `role_supplement: str = ""` (optional, defaults to empty string)
- Added optional `task: NodeTaskConfig | None = None` field to `NodeConfig`
- Exported `NodeTaskConfig` in `nodes/__init__.py`
- Created comprehensive test suite in `tests/unit/test_node_task_config.py` with 11 tests covering:
  - Minimal field instantiation with defaults
  - Full field instantiation
  - Serialization with `asdict()`
  - Field access
  - Equality comparisons
  - Integration with NodeConfig
  - Optional task field

### Files Modified
- `nodes/loader.py` - Added NodeTaskConfig dataclass and task field to NodeConfig
- `nodes/__init__.py` - Added NodeTaskConfig to exports

### Files Created
- `tests/unit/test_node_task_config.py` - Comprehensive test suite

### Test Results
- 11 tests passed
- Linting passed with ruff

---

*This story was created by SM Agent based on EPIC-21 requirements.*
