# Story 21.3: 重构 NodeConfig 结构

## Status

**Status**: Ready for Done

**Last Updated**: 2026-03-02 21:27:31

## Story
**As a** developer,
**I want** to refactor NodeConfig to remove deprecated fields and add new ones,
**So that** the configuration aligns with BMM requirements.

## Acceptance Criteria
- [x] **Given** the current NodeConfig dataclass **When** I remove description, questions, and dependencies fields **And** add task (NodeTaskConfig | None) and persona (dict | None) fields **Then** the dataclass can be instantiated with the new structure **And** existing tests that don't use deprecated fields still pass
- [x] **Given** a NodeConfig instance without task **When** I access the task property **Then** it returns None without error
- [x] **Given** a NodeConfig instance **When** I provide persona data as a dict **Then** it is stored correctly and accessible via the persona property
- [x] **Given** a NodeConfig instance with evaluator **When** I serialize to dict **Then** all fields including task, evaluator, and persona are properly exported
- [x] **Given** the refactored NodeConfig **When** I run type checking (basedpyright) **Then** no type errors are reported

## Tasks / Subtasks
- [x] Task 1: Update NodeConfig dataclass in loader.py to remove deprecated fields (description, questions, dependencies)
- [x] Task 2: Add new fields to NodeConfig: task (NodeTaskConfig | None), persona (dict[str, Any] | None)
- [x] Task 3: Update NodeConfig docstring to reflect new structure and BMM alignment
- [x] Task 4: Verify backward compatibility - ensure existing code that doesn't use deprecated fields continues to work
- [x] Task 5: Run basedpyright type checking to ensure type safety

## Dev Notes
- The deprecated fields (description, questions, dependencies) should be completely removed from the dataclass definition
- The new `task` field allows inline BMM task configuration per node
- The new `persona` field supports inline persona data without requiring separate persona files
- Both task and persona are optional to maintain backward compatibility
- The evaluator field remains unchanged as it was already optional
- Need to ensure that any existing code that creates NodeConfig instances is updated to use the new structure
- The persona field uses dict[str, Any] type to accommodate flexible persona configurations
- Consider adding type alias for persona type if more specific typing is needed in the future

## Testing
### Unit Tests
- [x] Test case 1: Create NodeConfig with new fields (task, persona) - verify all fields are accessible
- [x] Test case 2: Create NodeConfig without optional fields - verify task returns None, persona returns None
- [x] Test case 3: Verify NodeConfig serialization includes new fields when present
- [x] Test case 4: Verify type checking passes with basedpyright

### Integration Tests
- [x] Integration test 1: Load a node.yaml file and verify NodeConfig is created with correct task and persona data
- [x] Integration test 2: Verify NodeLoader.build_node_config() correctly handles new fields

### Manual Testing
- [x] Manual test 1: Create a test node with task configuration and verify it loads correctly
- [x] Manual test 2: Create a test node with inline persona and verify it loads correctly

## File List
- autoBMAD/nodes/loader.py - Modified: Added NodeTaskConfig, removed deprecated fields from NodeConfig, updated NodeLoader methods
- autoBMAD/nodes/__init__.py - Modified: Added NodeTaskConfig export
- tests/unit/test_node_config_refactor.py - Created: New test file for refactored NodeConfig
- tests/unit/test_node_task_config.py - Modified: Updated to use new NodeConfig structure
- tests/unit/test_node_deliverable_config.py - Modified: Updated to use new NodeConfig structure

---

## QA Results
- **Gate Decision**: PASS (100/100)
- **Reviewer**: Quinn - Test Architect
- **Review Date**: 2026-03-02
- **Type Checking**: PASS (0 errors)
- **Tests**: PASS (41 tests passed)
- **Document Completeness**: PASS (100%)
- **Notes**: Story implementation complete. Deprecated fields removed, new fields (task, persona) properly added with correct type hints. Backward compatibility maintained.

---
*This story was created by SM Agent based on EPIC-21 requirements.*
