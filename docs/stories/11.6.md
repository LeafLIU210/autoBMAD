# Story 11.6: Cleanup Dual Execution System

## Status

**Status**: Ready for Done

**Last Updated**: 2026-02-24 01:18:49

## Story
**As a** developer,
**I want** to remove the empty placeholder executor path and consolidate to a single execution system,
**So that** there is only one execution path and no confusion about which system is active in production code.

## Acceptance Criteria
- [x] `_create_default_node_executor()` marked as `@deprecated` with removal timeline documented in code comments
- [x] `create_pipeline_graph()` requires `session_manager` parameter (no `None` fallback) for production use, with clear type annotation
- [x] Test-only `MockNodeExecutor` available for unit tests that don't require real LLM execution
- [x] No code path produces empty `{}` deliverables - all paths go through integrated executor
- [x] All existing pipeline tests updated to use integrated executor or explicit mock
- [x] Warning logged when deprecated execution path is invoked
- [x] Type checking passes with `basedpyright` (zero new errors)

## Tasks / Subtasks
- [x] Task 1: Mark `_create_default_node_executor()` as `@deprecated` with removal timeline in docstring
- [x] Task 2: Add warning log (using `logging.warning`) when `session_manager=None` is passed to `create_pipeline_graph()`
- [x] Task 3: Update pipeline orchestrator to always provide session_manager (remove None fallback path)
- [x] Task 4: Create `MockNodeExecutor` class in test utils for unit testing without LLM
- [x] Task 5: Update existing pipeline tests in `tests/pipeline/` to use mock or integrated executor
- [x] Task 6: Run comprehensive grep search to verify no empty deliverable `{}` code paths remain

## Dev Notes
- **Deprecation Timeline**: Keep `_create_default_node_executor()` as deprecated for 2 sprint cycles before removal to allow migration period
- **Warning Message**: "WARNING: Using deprecated default node executor - this path produces empty {} deliverables and will be removed in a future release"
- **MockNodeExecutor Design**: Should return mock deliverables with placeholder content for testing pipeline flow without LLM
- **Backward Compatibility**: The deprecated path should still work but log warnings to alert developers during transition
- **Orchestrator Changes**: Requires updating `orchestrator.py` to call `self._get_or_create_session_manager()` before `create_pipeline_graph()`
- **Test Migration**: Existing tests using the old empty executor need to be updated to either use `MockNodeExecutor` or provide a mock session_manager

## Testing
### Unit Tests
- [x] Test that deprecated function logs warning when called
- [x] Test that MockNodeExecutor returns valid deliverable structure
- [x] Test that create_pipeline_graph raises TypeError when session_manager not provided (Python 3.10+)
- [x] Test that warning is logged when None session_manager is passed

### Integration Tests
- [x] Integration test verifying pipeline uses integrated executor path (not deprecated)
- [x] Integration test confirming no empty {} deliverables in final pipeline state

### Manual Testing
- [x] Run full pipeline and verify deliverables are non-empty
- [x] Check logs for deprecation warnings (should appear during test runs)
- [x] Verify basedpyright passes with zero new errors

---
*Story prepared by SM Agent based on Epic 11 (Node Executor Integration Fix)*
