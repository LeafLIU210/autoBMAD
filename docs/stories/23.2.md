# Story 23.2: 移除 graph.py 废弃函数

## Status

**Status**: Ready for Done

**Last Updated**: 2026-03-02 23:19:22

## Story
**As a** developer,
**I want** to remove deprecated functions from graph.py,
**So that** only current executor logic remains.

## Acceptance Criteria

### 23.2.1 Remove _create_default_node_executor Function
- [x] The graph.py file does not contain `_create_default_node_executor` function definition
- [x] No other code in the codebase imports or references `_create_default_node_executor`
- [x] basedpyright reports no type errors after removal

### 23.2.2 Remove create_enhanced_node_executor Function
- [x] The graph.py file does not contain `create_enhanced_node_executor` function definition
- [x] No other code in the codebase imports or references `create_enhanced_node_executor`
- [x] basedpyright reports no type errors after removal

### 23.2.3 Make session_manager Required
- [x] The `create_pipeline_graph` function signature shows `session_manager: KimiSessionManager` (no default value)
- [x] The function no longer has fallback logic for `None` session_manager
- [x] All callers of `create_pipeline_graph` provide the session_manager argument

## Tasks / Subtasks

### Task 1: Remove _create_default_node_executor Function
- [x] 1.1 Locate the function definition in graph.py (expected around lines 56-159)
- [x] 1.2 Delete the entire function definition
- [x] 1.3 Remove any imports of this function if present in other files
- [x] 1.4 Run grep to verify no references remain: `grep -r "_create_default_node_executor" autoBMAD/`

### Task 2: Remove create_enhanced_node_executor Function
- [x] 2.1 Locate the function definition in graph.py (expected around lines 473-489)
- [x] 2.2 Delete the entire function definition
- [x] 2.3 Verify no other files import or reference this function

### Task 3: Update create_pipeline_graph session_manager Parameter
- [x] 3.1 Locate the `create_pipeline_graph` function definition
- [x] 3.2 Change `session_manager: KimiSessionManager = None` to `session_manager: KimiSessionManager`
- [x] 3.3 Remove any fallback logic that handles None session_manager
- [x] 3.4 Update all callers to provide session_manager argument

### Task 4: Run Type Checking
- [x] 4.1 Run basedpyright on graph.py: `basedpyright autoBMAD/docuswarm/pipeline/graph.py`
- [x] 4.2 Fix any type errors if they arise

### Task 5: Verify Changes
- [x] 5.1 Run grep to ensure no deprecated function references remain
- [x] 5.2 Run existing tests to ensure no regressions

## Dev Notes

### Technical Considerations
- **Dependency Impact**: Before removal, verify that no other modules call these deprecated functions
- **Session Manager**: The `create_pipeline_graph` function is likely called from multiple places; ensure all callers are updated to pass session_manager
- **Type Safety**: After making session_manager required, callers must provide a valid KimiSessionManager instance

### Code Patterns to Remove
```python
# Remove this entire function (~lines 56-159):
def _create_default_node_executor(...):
    """..."""

# Remove this entire function (~lines 473-489):
def create_enhanced_node_executor(...):
    """..."""

# Update this signature:
# Before: def create_pipeline_graph(..., session_manager: KimiSessionManager = None)
# After:  def create_pipeline_graph(..., session_manager: KimiSessionManager)
```

### Fallback Logic to Remove
Look for patterns like:
```python
if session_manager is None:
    session_manager = KimiSessionManager(...)
```

### Files to Check for References
- `pipeline/graph.py` - main file
- `pipeline/__init__.py` - exports
- Any test files that might reference these functions
- Any caller files that use `create_pipeline_graph`

## Testing

### Unit Tests
- [x] Verify `_create_default_node_executor` is not accessible from graph module
- [x] Verify `create_enhanced_node_executor` is not accessible from graph module
- [x] Verify `create_pipeline_graph` raises TypeError when called without session_manager
- [x] Verify `create_pipeline_graph` works correctly with session_manager argument

### Integration Tests
- [x] Run existing pipeline tests to ensure graph construction still works
- [x] Run full test suite to verify no regressions

### Manual Testing
- [x] Import graph module in Python REPL and verify functions don't exist
- [x] Run a simple pipeline execution to verify functionality

## Completion Notes

### Changes Made
1. Removed `_create_default_node_executor` function from `autoBMAD/docuswarm/pipeline/graph.py`
2. Removed `create_enhanced_node_executor` function from `autoBMAD/docuswarm/pipeline/graph.py`
3. Made `session_manager` a required parameter in `create_pipeline_graph` function
4. Updated `create_graph_with_checkpointer` to also require `session_manager`
5. Removed unused imports (`warnings`, `validate_deliverable_format`)
6. Created comprehensive test suite in `tests/unit/test_story_23_2_graph_deprecated_removal.py`

### Test Results
- All 651 tests pass (2 skipped - unrelated to changes)
- basedpyright reports 0 errors, 0 warnings for pipeline module

### Files Modified
- `autoBMAD/docuswarm/pipeline/graph.py` - Removed deprecated functions, made session_manager required

### Files Added
- `tests/unit/test_story_23_2_graph_deprecated_removal.py` - Comprehensive test suite

---

## QA Results

**Gate Decision**: PASS
**Reviewer**: Quinn (Test Architect)
**Date**: 2026-03-02

### Verification Summary

| Check | Result |
|-------|--------|
| Deprecated function `_create_default_node_executor` removed | PASS |
| Deprecated function `create_enhanced_node_executor` removed | PASS |
| `session_manager` parameter is now required | PASS |
| No fallback logic for None session_manager | PASS |
| basedpyright type check | PASS (0 errors, 0 warnings) |
| Test suite execution | PASS (651 passed, 2 skipped) |

### Acceptance Criteria Status
- 23.2.1 Remove _create_default_node_executor Function: PASS
- 23.2.2 Remove create_enhanced_node_executor Function: PASS
- 23.2.3 Make session_manager Required: PASS

---

*Story implemented by Dev Agent - Ready for Review*
