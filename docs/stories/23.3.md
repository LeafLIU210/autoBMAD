# Story 23.3: 移除 dual_agent.py 冗余函数

## Status

**Status**: Ready for Done

**Last Updated**: 2026-03-02 23:19:15

## Story
**As a** developer,
**I want** to remove redundant functions from dual_agent.py,
**So that** it only contains the DualAgentNode class and eliminates dead code.

## Acceptance Criteria
- [x] **Given** the dual_agent.py file contains create_node_executor (lines ~836-868)
  - **When** I remove the entire function definition
  - **Then** the function no longer exists in the module

- [x] **Given** the dual_agent.py file contains _execute_node (lines ~871-968)
  - **When** I remove the entire function definition
  - **Then** the function no longer exists in the module

- [x] **Given** the dual_agent.py file contains _get_config (lines ~971-991)
  - **When** I remove the entire function definition
  - **Then** the function no longer exists in the module

- [x] **Given** the __all__ export list in dual_agent.py
  - **When** I remove the deleted function names from __all__
  - **Then** __all__ only contains existing exports

- [x] **Given** the code references to removed functions are cleaned up
  - **When** I verify no imports or calls remain
  - **Then** basedpyright reports no import errors

## Tasks / Subtasks
- [x] **Task 1**: Remove create_node_executor function (lines ~836-868)
  - [x] Identify exact line numbers in dual_agent.py
  - [x] Delete the entire function definition
  - [x] Remove any type hints or helper functions only used by this function

- [x] **Task 2**: Remove _execute_node function (lines ~871-968)
  - [x] Identify exact line numbers in dual_agent.py
  - [x] Delete the entire function definition
  - [x] Clean up any nested helper functions

- [x] **Task 3**: Remove _get_config function (lines ~971-991)
  - [x] Identify exact line numbers in dual_agent.py
  - [x] Delete the entire function definition

- [x] **Task 4**: Update __all__ export list
  - [x] Remove 'create_node_executor' from __all__
  - [x] Remove '_execute_node' from __all__
  - [x] Remove '_get_config' from __all__
  - [x] Verify only existing exports remain in __all__

- [x] **Task 5**: Verify no broken imports
  - [x] Run basedpyright on dual_agent.py
  - [x] Search for any remaining references to removed functions in the codebase

- [x] **Task 6**: Run tests to confirm no regressions
  - [x] Execute existing dual_agent tests
  - [x] Verify pipeline still functions correctly

## Dev Notes
- **Duplicate Logic**: These three functions (create_node_executor, _execute_node, _get_config) duplicate functionality already present in `executor.py`. Keeping both creates maintenance burden.

- **No External Calls**: Verify using grep that no other modules import or call these functions:
  ```
  grep -r "create_node_executor" autoBMAD/ --include="*.py"
  grep -r "_execute_node" autoBMAD/ --include="*.py"
  grep -r "_get_config" autoBMAD/ --include="*.py"
  ```

- **DualAgentNode Class**: This is the main class that should remain. It uses internal execution logic that doesn't depend on these removed functions.

- **Ralph's Principles Applied**:
  - **DRY**: Remove duplicated code between executor.py and dual_agent.py
  - **KISS**: Simpler module with only necessary code
  - **YAGNI**: These functions are never called externally
  - **Occam's Razor**: Fewer functions = fewer things to understand

## Testing

### Unit Tests
- [ ] **Test 1**: Verify create_node_executor is not in dual_agent module
  ```python
  from docuswarm.nodes import dual_agent
  assert not hasattr(dual_agent, 'create_node_executor')
  ```

- [ ] **Test 2**: Verify _execute_node is not in dual_agent module
  ```python
  from docuswarm.nodes import dual_agent
  assert not hasattr(dual_agent, '_execute_node')
  ```

- [ ] **Test 3**: Verify _get_config is not in dual_agent module
  ```python
  from docuswarm.nodes import dual_agent
  assert not hasattr(dual_agent, '_get_config')
  ```

- [ ] **Test 4**: Verify __all__ only contains valid exports
  ```python
  from docuswarm.nodes import dual_agent
  expected_exports = ['DualAgentNode']
  assert dual_agent.__all__ == expected_exports
  ```

### Integration Tests
- [ ] **Integration Test 1**: DualAgentNode can still be imported and instantiated
- [ ] **Integration Test 2**: Pipeline graph construction works with remaining code
- [ ] **Integration Test 3**: Node execution still functions correctly

### Manual Testing
- [ ] **Manual Test 1**: Run full test suite to ensure no regressions
- [ ] **Manual Test 2**: Verify basedpyright passes on dual_agent.py
- [ ] **Manual Test 3**: Check that no IDE shows broken imports

---

## Dev Agent Record

### Implementation Summary
- Removed 3 redundant functions from dual_agent.py: create_node_executor, _execute_node, _get_config
- Updated __all__ to remove the deleted function exports
- Updated nodes/__init__.py to remove create_node_executor import
- Created comprehensive test suite: tests/nodes/test_dual_agent_removal.py

### Changes Made
1. **dual_agent.py** (lines 824-991 removed)
   - Removed create_node_executor function (~33 lines)
   - Removed _execute_node function (~98 lines)
   - Removed _get_config function (~21 lines)
   - Updated __all__ to remove create_node_executor
   - Removed unused PipelineState import

2. **nodes/__init__.py**
   - Removed create_node_executor from imports
   - Removed create_node_executor from __all__

3. **tests/nodes/test_dual_agent_removal.py** (NEW)
   - Created 14 comprehensive tests verifying function removal
   - All tests pass

### Verification
- All 665 tests pass (1 skipped)
- basedpyright: 0 errors, 0 warnings on dual_agent.py
- No import errors in the codebase

### Agent Model Used
Claude Sonnet 4.6

---

*This story was created by SM Agent (Bob) following the create-next-story procedure.*
