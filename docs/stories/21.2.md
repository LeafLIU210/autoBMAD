# Story 21.2: 扩展 NodeDeliverableConfig

## Status
**Status**: Ready for Done

**Last Updated**: 2026-03-02 21:27:46

## Story
**As a** developer,
**I want** to extend NodeDeliverableConfig with new fields (template_title and output_filename),
**So that** we can support BMM-aligned template titles and output filename patterns for deliverables.

## Acceptance Criteria
- [x] **AC1 - Backward Compatibility**: Given a NodeDeliverableConfig instantiated with only required fields (type, required_sections), when the config is created, then it works without errors and template_title/output_filename default to empty strings
- [x] **AC2 - New Fields Support**: Given a NodeDeliverableConfig with template_title="Product Brief: {project_name}" and output_filename="product-brief-{project_name}.md", when I access these properties, then the placeholders are preserved exactly as provided
- [x] **AC3 - YAML Population**: Given a YAML config with template_title and output_filename fields, when NodeLoader loads the config, then these values are correctly populated in the NodeDeliverableConfig instance
- [x] **AC4 - Serialization**: Given a NodeDeliverableConfig instance with all fields populated, when serializing to dict, then template_title and output_filename are included in the output
- [x] **AC5 - Type Safety**: Given basedpyright type checking on the modified loader.py, when running the type checker, then no type errors are reported for the new fields

## Tasks / Subtasks
- [x] **Task 1**: Add template_title and output_filename fields to NodeDeliverableConfig dataclass in `nodes/loader.py`
  - [x] Subtask 1.1: Add `template_title: str = ""` field with empty string default
  - [x] Subtask 1.2: Add `output_filename: str = ""` field with empty string default
  - [x] Subtask 1.3: Update dataclass docstring to document new attributes
- [x] **Task 2**: Update NodeLoader._build_node_config() to parse new fields
  - [x] Subtask 2.1: Extract template_title from yaml_data["deliverable"]
  - [x] Subtask 2.2: Extract output_filename from yaml_data["deliverable"]
  - [x] Subtask 2.3: Pass values to NodeDeliverableConfig constructor
- [x] **Task 3**: Run type checker to ensure no type errors
  - [x] Subtask 3.1: Run `basedpyright nodes/loader.py`
  - [x] Subtask 3.2: Fix any reported type issues

## Dev Notes
- **Backward Compatibility**: The new fields must have empty string defaults to ensure existing code continues to work without modification
- **Placeholder Preservation**: The template_title and output_filename fields should preserve placeholders like `{project_name}` exactly as provided - no string interpolation should happen during loading
- **Field Order**: Place new fields after existing required fields in the dataclass definition for clean code organization
- **Consistency**: Follow the same pattern as other optional fields in the codebase (e.g., role_supplement in NodeTaskConfig)
- **Documentation**: Update the class docstring to describe the new fields and their purpose

## Testing
### Unit Tests
- [x] **UT1**: Test minimal instantiation - create NodeDeliverableConfig with only type and required_sections, verify template_title and output_filename default to ""
- [x] **UT2**: Test full instantiation - create NodeDeliverableConfig with all fields including new ones, verify values are correctly stored
- [x] **UT3**: Test placeholder preservation - verify that "{project_name}" placeholder is NOT interpolated or modified
- [x] **UT4**: Test serialization - convert NodeDeliverableConfig to dict, verify template_title and output_filename are in the output

### Integration Tests
- [x] **IT1**: Test NodeLoader loading a YAML with new deliverable fields, verify values are correctly parsed
- [x] **IT2**: Test backward compatibility - load a YAML config without new fields, verify defaults work correctly
- [x] **IT3**: Test full node loading with new deliverable fields end-to-end

### Manual Testing
- [x] **MT1**: Manually verify that loader.py can be imported without errors
- [x] **MT2**: Create a test node.yaml with new fields and verify loading works via Python REPL

## File List
- **Modified**: `nodes/loader.py` - Added template_title and output_filename fields to NodeDeliverableConfig, updated _build_node_config() to parse new fields
- **Created**: `tests/unit/test_node_deliverable_config.py` - Comprehensive test suite with 17 tests covering all acceptance criteria

## Agent Model Used
Claude Sonnet 4.6

---
*This story was created by SM Agent based on EPIC-21 requirements.*
