# Story 11.5: End-to-End Integration Test

## Status

**Status**: Ready for Development

**Last Updated**: 2026-02-24 01:18:59

## Story
**As a** developer,
**I want** to have an integration test verifying the complete flow from proposal to file output,
**So that** I can confirm the integration gap is fixed and prevent regression.

## Acceptance Criteria
- [ ] Test creates a proposal context file
- [ ] Test executes at least one node (analyst) through pipeline
- [ ] Test verifies: deliverable is non-empty in pipeline state
- [ ] Test verifies: markdown file exists in `output/{pipeline_id}/`
- [ ] Test verifies: file content is non-empty and valid markdown
- [ ] Test verifies: CreateDeliverableTool was invoked (not empty placeholder)
- [ ] Test uses `@pytest.mark.integration` marker
- [ ] Test cleans up output directory after execution

## Tasks / Subtasks
- [ ] Task 1: Create `tests/integration/test_node_executor_integration.py` test file with proper imports and markers
- [ ] Task 2: Implement test fixture to create temporary proposal context with sample subject
- [ ] Task 3: Implement test function `test_analyst_node_produces_deliverable()` to execute single node through pipeline
- [ ] Task 4: Implement assertions to verify no empty deliverables (`{}`) in pipeline state
- [ ] Task 5: Implement assertions to verify markdown file exists in correct output directory
- [ ] Task 6: Implement assertions to verify file content is non-empty and valid markdown (>100 chars)
- [ ] Task 7: Add cleanup fixture using `shutil.rmtree` to remove output directory after test execution
- [ ] Task 8: Add `@pytest.mark.integration` marker and `@pytest.mark.asyncio` for async test support
- [ ] Task 9: Add test for multiple node executions producing isolated files
- [ ] Task 10: Add graceful failure handling when API key is missing

## Dev Notes
- The test requires the Kimi K2.5 API key to be available in environment variable `KIMI_API_KEY`
- Use `uuid.uuid4().hex[:8]` to generate unique pipeline IDs to avoid test collisions
- Output directory path: `Path("output") / pipeline_id` where pipeline_id follows format `test-{8-char-hex}`
- The test must verify that `CreateDeliverableTool` was actually invoked, not just that the deliverable exists in state
- Use `shutil.rmtree()` in a `finally` block to ensure cleanup even if assertions fail
- Mock or skip network calls if API key is not available using `pytest.skip` with clear message
- The test file should be placed in `tests/integration/` directory following project test structure
- Import `execute_single_node` from `autoBMAD.docuswarm.node_execution.executor` or similar module
- Consider using `pytest-asyncio` for async test support

## Testing
### Unit Tests
- [ ] Test state conversion functions `_convert_pipeline_to_node_state()` and `_convert_node_to_pipeline_state()`
- [ ] Test CreateDeliverableTool with mocked work_dir
- [ ] Test session creation with agent_file and work_dir parameters

### Integration Tests
- [ ] Integration test: `test_analyst_node_produces_deliverable()` - verifies single node produces non-empty deliverable file
- [ ] Integration test: `test_multiple_nodes_produce_isolated_files()` - verifies multiple nodes write to same output directory with different filenames
- [ ] Integration test: `test_no_empty_deliverables()` - verifies no `{}` placeholders in pipeline state

### Manual Testing
- [ ] Manual test: Run full pipeline with real API key and verify all 5 nodes produce deliverables
- [ ] Manual test: Verify file output in `output/{pipeline_id}/` directory matches deliverable content
- [ ] Manual test: Check that no empty `{}` deliverables appear in pipeline execution logs

---
*This story was created by SM Agent based on Epic 11: Node Executor Integration Fix.*
