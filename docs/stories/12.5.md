# Story 12.5: Orchestrator 重构

## Status

**Status**: Ready for Done

**Last Updated**: 2026-03-02 00:48:51

## Story
**As a** developer,
**I want to** 重构 HybridOrchestrator 类使用 CheckpointManager
**So that** 消除 4 处重复的 checkpointer 创建代码，提高代码可维护性

## Acceptance Criteria
- [x] 在 `__init__` 方法中初始化 CheckpointManager 实例
- [x] `start_pipeline` 方法使用 CheckpointManager.get_or_create() 获取 checkpointer
- [x] `resume_pipeline` 方法使用 CheckpointManager.get_or_create() 获取 checkpointer
- [x] `restart_from_node` 方法使用 CheckpointManager.get_or_create() 获取 checkpointer
- [x] `_restart_node` 方法使用 CheckpointManager.get_or_create() 获取 checkpointer
- [x] 删除所有 4 处重复的 checkpointer 创建逻辑（PRAGMA 设置等）
- [x] orchestrator.py 文件行数减少至少 60 行

## Tasks / Subtasks
- [x] Task 1: 在 HybridOrchestrator.__init__ 中添加 CheckpointManager 初始化
  - 导入 CheckpointManager 类
  - 创建 self._checkpoint_manager 实例，传入 db_path 和 external_checkpointer 参数
- [x] Task 2: 重构 start_pipeline 方法
  - 移除内联的 checkpointer 创建代码
  - 调用 await self._checkpoint_manager.get_or_create(final_pipeline_id)
  - 更新日志记录
- [x] Task 3: 重构 resume_pipeline 方法
  - 移除内联的 checkpointer 创建代码
  - 调用 await self._checkpoint_manager.get_or_create(pipeline_id)
  - 更新日志记录
- [x] Task 4: 重构 restart_from_node 方法
  - 移除内联的 checkpointer 创建代码
  - 调用 await self._checkpoint_manager.get_or_create(pipeline_id)
  - 更新日志记录
- [x] Task 5: 重构 _restart_node 方法
  - 移除内联的 checkpointer 创建代码
  - 调用 await self._checkpoint_manager.get_or_create(pipeline_id)
  - 更新日志记录
- [x] Task 6: 验证 PRAGMA journal_mode 在 orchestrator.py 中出现次数为 0
- [x] Task 7: 运行 basedpyright 确保类型检查通过
- [x] Task 8: 运行 ruff 确保代码风格合规

## Dev Notes
- CheckpointManager 需要在 HybridOrchestrator 初始化时创建，并保存对 self._db_path 和 self._checkpointer 的引用
- 所有使用 checkpointer 的方法都需要改为异步调用 get_or_create()
- 确保 close() 方法在适当的时机被调用以释放资源（可选：添加 close() 调用逻辑）
- 需要导入 generate_thread_id 和 create_checkpoint_config 辅助函数（如果尚未导入）
- 日志记录应保持一致性，使用 structlog 的 bind 方法
- 注意保留原有的错误处理逻辑

## Testing

### Unit Tests
- [x] 验证 CheckpointManager 在 orchestrator 中正确初始化
- [x] 验证 start_pipeline 方法调用 get_or_create
- [x] 验证 resume_pipeline 方法调用 get_or_create
- [x] 验证 restart_from_node 方法调用 get_or_create
- [x] 验证 _restart_node 方法调用 get_or_create

### Integration Tests
- [x] 运行现有集成测试确保重构未引入回归
- [x] 测试完整 pipeline 生命周期（start -> resume -> restart）

### Manual Testing
- [ ] 手动测试 start_pipeline 功能正常
- [ ] 手动测试 resume_pipeline 功能正常
- [ ] 手动测试 restart_from_node 功能正常

## File List
- Modified: autoBMAD/docuswarm/pipeline/orchestrator.py
- Added: tests/unit/test_orchestrator_checkpoint_manager.py

## Change Log
- 2026-03-02: Refactored HybridOrchestrator to use CheckpointManager
  - Added CheckpointManager initialization in __init__
  - Replaced 4 duplicate checkpointer creation code blocks with CheckpointManager.get_or_create() calls
  - Removed unused imports (generate_thread_id, create_checkpoint_config)
  - Reduced orchestrator.py from 1134 to 1043 lines (91 lines reduction, exceeds 60-line requirement)
  - All 35 unit tests pass
  - basedpyright: 0 errors, 0 warnings
  - ruff: passes

---

## QA Results
**Reviewer**: Quinn (Test Architect)
**Date**: 2026-03-02
**Decision**: PASS

### Quality Gate Summary
- **basedpyright**: PASS (0 errors, 0 warnings)
- **ruff**: PASS (0 errors)
- **pytest**: PASS (35 tests passed)
  - test_orchestrator_checkpoint_manager.py: 11 tests
  - test_checkpoint_manager.py: 24 tests

### Acceptance Criteria Verification
All 7 acceptance criteria verified:
1. CheckpointManager initialized in `__init__` - VERIFIED
2. start_pipeline uses get_or_create() - VERIFIED
3. resume_pipeline uses get_or_create() - VERIFIED
4. restart_from_node uses get_or_create() - VERIFIED
5. _restart_node uses get_or_create() - VERIFIED
6. PRAGMA occurrences = 0 - VERIFIED
7. Line reduction: 92 lines (exceeds 60-line requirement) - VERIFIED

### Gate File
Created: `docs/qa/gates/12.5-orchestrator-refactor.yml`

---
*This story was created by SM Agent for EPIC-12 CheckpointManager Refactor.*
